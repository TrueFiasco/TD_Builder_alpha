{
  "_comment": "TD Builder V0.1.1 client config template. Replace <RELEASE_ROOT> with the absolute path to this release folder (the parent of this MCP/ directory). Register td-builder always; add td-builder-live only when TouchDesigner is open.",
  "mcpServers": {
    "td-builder": {
      "command": "python",
      "args": ["<RELEASE_ROOT>/MCP/server.py"],
      "env": {
        "PYTHONIOENCODING": "utf-8",
        "TD_BUILDER_ROOT": "<RELEASE_ROOT>"
      }
    },
    "td-builder-live": {
      "command": "python",
      "args": ["<RELEASE_ROOT>/MCP/live_server.py"],
      "env": {
        "PYTHONIOENCODING": "utf-8",
        "TD_API_URL": "http://127.0.0.1:9981"
      }
    }
  }
}
