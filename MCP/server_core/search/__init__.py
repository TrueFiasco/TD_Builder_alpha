"""Search package for the MCP server (unified_search + retrieval_stack).

Import submodules directly — e.g. ``from search.unified_search import
UnifiedSearchAdapter`` — as eval/run_eval.py and eval/pool_diag.py do; this
__init__ deliberately exports nothing.
"""
