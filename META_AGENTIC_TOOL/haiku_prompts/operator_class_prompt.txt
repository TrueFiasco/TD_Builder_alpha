# TouchDesigner Python API - Semantic Descriptions

For each method/member below, write a 1-2 sentence semantic description.
Focus on WHAT it does and WHEN you'd use it.
Write for someone searching 'how do I...' - use natural language.

Output format:
```yaml
class_name:
  method_name: "description..."
```

---

## AbsTime_Class
Operator: AbsTime ()

### Members
- `frame` → float (read-only)
  Raw: Absolute time elapsed between start of previous and current frame.

- `seconds` → float (read-only)
  Raw: Absolute total seconds played since the application started. Paused only with the power On/Off or with power().

- `step` → float (read-only)
  Raw: Number of absolute frames elapsed between start of previous and current frame. When this value is greater than 1, the system is dropping frames.

- `stepSeconds` → float (read-only)
  Raw: Absolute time elapsed between start of previous and current frame.

## ActorCOMP_Class
Operator: ActorCOMP (COMP)

### Members
- `bodies` → list (read-only)
  Raw: Returns a list of all the bodies in the

## AnimationCOMP_Class
Operator: AnimationCOMP (COMP)

### Methods
- `setKeyframe(position, channel='*', value=None, function=None)` → None
  Raw: Remove a keyframe from an animation channel.

- `deleteKeyframe(position, channel='*', value=None, function=None)` → None
  Raw: Remove a keyframe from an animation channel.

## AnnotateCOMP_Class
Operator: AnnotateCOMP (COMP)

### Members
- `enclosedOPs` → List[OP] (read-only)
  Raw: The annotation's bottom edge, as measured in network editor units.

- `height` → float (read-only)
  Raw: The annotation's height, as measured in network editor units.

- `utility` → bool
  Raw: Get or set utility flag. Controls whether or not it is found

- `width` → float (read-only)
  Raw: The annotation's width, as measured in network editor units.

- `x` → float (read-only)
  Raw: The annotation's left edge, as measured in network editor units.

- `y` → float (read-only)
  Raw: The annotation's bottom edge, as measured in network editor units.

## App_Class
Operator: App ()

### Members
- `applicationsFolder` → str (read-only)
  Raw: Removes the restriction previously added. Returns True if successful.

- `architecture` → str (read-only)
  Raw: The architecture of the compile. Generally 32 or 64 bit.

- `binFolder` → str (read-only)
  Raw: Installation folder containing the binaries.

- `build` → str (read-only)
  Raw: Application build number.

- `compileDate` → Tuple[int, int, int] (read-only)
  Raw: The date the application was compiled, expressed as a tuple (year, month, day).

- `configFolder` → str (read-only)
  Raw: Installation folder containing configuration files.

- `desktopFolder` → str (read-only)
  Raw: Current user's desktop folder.

- `enableCachedParameters` → bool
  Raw: Get or set caching parameter values instead of always evaluating.

- `enableOptimizedExprs` → bool
  Raw: Get or set if Python expression optimization is enabled. Defaults to True every time TouchDesigner starts.

- `experimental` → bool (read-only)
  Raw: Returns true if the App is an experimental build, false otherwise.

- `installFolder` → str (read-only)
  Raw: Main installation folder.

- `launchTime` → float (read-only)
  Raw: Total time required to launch and begin playing the toe file, measured in seconds.

- `logExtensionCompiles` → bool
  Raw: Get or set if extra messages for starting and ending compiling extensions is sent to the textport. Additional error stack will be printed if compilation fails. Defaults to False every time TouchDesign

- `osName` → str (read-only)
  Raw: The operating system name.

- `osVersion` → str (read-only)
  Raw: The operating system version.

- `power` → bool
  Raw: Get or set the overall processing state of the process. When True, processing is enabled. When False processing is halted. This is identical to pressing the power button on the main interface. This ha

- `preferencesFolder` → str (read-only)
  Raw: The ID of the current running process.

- `product` → str (read-only)
  Raw: Type of executable the project is running under. Values are 'TouchDesigner', 'TouchPlayer' or 'TouchEngine'.

- `pythonExecutable` → str (read-only)
  Raw: Get or set the list of most recently saved or loaded files.

- `samplesFolder` → str (read-only)
  Raw: Installation folder containing configuration files.

- `paletteFolder` → str (read-only)
  Raw: Installation folder containing palette files.

- `userPaletteFolder` → str (read-only)
  Raw: Application version number.

- `windowColorBits` → int (read-only)
  Raw: The number of color bits per color channel the TouchDesigner window is running at. By default this will be 8-bits per channel, but can be increased to 10-bits by settings env var TOUCH_10_BIT_COLOR=1.

- `systemFolder` → str (read-only)
  Raw: Installation folder containing system files.

- `tempFolder` → str (read-only)
  Raw: Returns access to the global

### Methods
- `addNonCommercialLimit(password=None)` → None
  Raw: Limits the application to operate at non-commercial license level. Multiple calls can be made, but each can be undone with a matching removeNonCommercialLimit(password). If the password is blank the o

- `removeNonCommercialLimit(password=None)` → bool
  Raw: Removes the restriction previously added. Returns True if successful.

- `addResolutionLimit(x, y, password=None)` → None
  Raw: Limits all textures to the specified amount. Multiple calls can be made, but each can be undone with a matching removeResolutionLimit(password). The final resolution limit will be the minimum of all c

- `removeResolutionLimit(password=None)` → bool
  Raw: Removes the restriction previously added. Returns True if successful.

## ArcBall_Class
Operator: ArcBall ()

### Methods
- `beginPan(u, v)` → None
  Raw: Resets all values of the ArcBall to the default state.

- `beginRotate(u, v)` → None
  Raw: Begin an arcball rotation at the given u and v.

- `beginDolly(u, v)` → None
  Raw: Begin a dolly at at the given u and v.

- `pan(u, v)` → None
  Raw: Pan the view by the given x and y.

- `panTo(u, v, scale=1.0)` → None
  Raw: Pan from the u,v given in the last call to beginPan() to the given u and v, applying a scale as well to the pan amount.

- `rotateTo(u, v, scale=1.0)` → None
  Raw: Rotates the arcball to the given u and v position.

- `dolly(z)` → None
  Raw: Dolly the view by the given z value.

- `dollyTo(u, v, scale=1.0)` → None
  Raw: Dolly from the u,v given in the last call to beginDolly() to the given u and v, applying a scale as well to the dolly amount.(Keyword, Optional)

- `transform()` → TDU.Matrix
  Raw: Gets the current transform

- `identity()` → None
  Raw: Resets all values of the ArcBall to the default state.

## Attribute_Class
Operator: Attribute ()

### Members
- `owner` → OP (read-only)
  Raw: Returns the attribute values as a list.

- `name` → str (read-only)
  Raw: The name of this attribute.

- `size` → int (read-only)
  Raw: The number of values associated with this attribute. For example, a normal attribute has a size of 3.

- `type` → float | int | str | tuple | TDU.Position | TDU.Vector (read-only)
  Raw: The type associated with this attribute: float, integer, string, tuple,

- `default` → float | int | str | tuple | TDU.Position | TDU.Vector (read-only)
  Raw: The default values associated with this attribute. Dependent on the type of attribute, it may return a float, integer, string, tuple,

- `isArray` → bool (read-only)
  Raw: True if the attribute is an an array.

- `arraySize` → int (read-only)
  Raw: The size of the array for array attributes, 0 otherwise.

- `numMatCols` → int (read-only)
  Raw: The number of columns for matrix attributes, 0 otherwise.

- `numMatRows` → int (read-only)
  Raw: The number of rows for matrix attributes, 0 otherwise.

### Methods
- `destroy()` → None
  Raw: Destroy the attribute referenced by this object.

- `vals(delayed=False)` → list
  Raw: Returns the attribute values as a list.

## AttributeData_Class
Operator: AttributeData ()

### Members
- `owner` → OP (read-only)
  Raw: The set of values contained within this object. Dependent on the type of attribute, it may return a float, integer, string, tuple,

- `val` → float | int | str | tuple | TDU.Position | TDU.Vector (read-only)
  Raw: The set of values contained within this object. Dependent on the type of attribute, it may return a float, integer, string, tuple,

## Attributes_Class
Operator: Attributes ()

### Members
- `owner` → OP (read-only)
  Raw: Create a new

- `[name]` → Attribute
  Raw: Create a new

## AudiobandeqCHOP_Class
Operator: AudiobandeqCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## AudiodynamicsCHOP_Class
Operator: AudiodynamicsCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## AudiofileinCHOP_Class
Operator: AudiofileinCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Get a dictionary containing all the metadata.

- `metadata` → dict (read-only)
  Raw: Get a dictionary containing all the metadata.

## AudiofilterCHOP_Class
Operator: AudiofilterCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## AudiomovieCHOP_Class
Operator: AudiomovieCHOP (CHOP)

### Members
- `hasAudio` → bool (read-only)
  Raw: The current movie playback rate.

- `playbackRate` → float (read-only)
  Raw: The current movie playback rate.

## AudioparaeqCHOP_Class
Operator: AudioparaeqCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## AudioplayCHOP_Class
Operator: AudioplayCHOP (CHOP)

### Methods
- `play(index, start=True, loop=False,  delaySeconds=0.0, pan=None, rolloff=None, volume=None, fadeSeconds=0.0)` → None
  Raw: Stop the playing of audio samples specified in an

- `stop(index)` → None
  Raw: Stop the playing of audio samples specified in an

## AudiovstCHOP_Class
Operator: AudiovstCHOP (CHOP)

### Methods
- `panic()` → None
  Raw: Sends a Program Change event through the

- `send(*messages, timestamp=0)` → None
  Raw: Send a sequence of 3 MIDI bytes through this

- `sendAfterTouch(channel, note, velocity)` → None
  Raw: Sends an After Touch event through the

- `sendAllNotesOff(channel)` → None
  Raw: Sends a All Notes Off event through the

- `sendChannelPressure(channel, value)` → None
  Raw: Sends a Channel Pressure event through the

- `sendControl(channel, index, value)` → None
  Raw: Sends a Controller event through the

- `sendExclusive(*messages, timestamp=0)` → None
  Raw: Send a sequence of exclusive MIDI bytes through this

- `sendNoteOff(channel, note, velocity)` → None
  Raw: Sends a Note On event through the

- `sendNoteOn(channel, note, velocity, noteOffDelay=None, noteOffVelocity=None)` → None
  Raw: Sends a Note On event through the

- `sendPitchBend(channel, value)` → None
  Raw: Sends a Pitch Bend event through the

- `sendProgram(channel, value)` → None
  Raw: Sends a Program Change event through the

## Bezier_Class
Operator: Bezier ()

### Members
- `anchors` → list (read-only)
  Raw: Deletes the anchor and its neighbouring tangents.

- `basis` → list (read-only)
  Raw: Return the bezier basis as a list of float values.

- `closed` → bool
  Raw: Get or set whether the curve is closed or open.

- `order` → float (read-only)
  Raw: Return the bezier order. The order is one more than the degree.

- `segments` → list (read-only)
  Raw: Returns a list of segments, where each segment is a list of

- `tangents` → list (read-only)
  Raw: Returns the tangents as a list of

### Methods
- `insertAnchor(u)` → Vertex
  Raw: inserts anchor at given position (u from 0..1) and returns anchor vertex.

- `updateAnchor(anchorIndex, targetPosition, tangents=True)` → tdu.Position
  Raw: Modify the anchor vertex to the new

- `appendAnchor(targetPosition, preserveShape=True)` → Vertex
  Raw: Appends a set of vertices, creating a new segment on the curve, ending with the targetPosition.

- `updateTangent(tangentIndex, targetPosition, rotate=True, scale=True, rotateLock=True, scaleLock=True)` → tdu.Position
  Raw: Modify the vertex vertex to the new

- `deleteAnchor(anchorIndex)` → None
  Raw: Deletes the anchor and its neighbouring tangents.

## Body_Class
Operator: Body ()

### Members
- `index` → int (read-only)
  Raw: Applies force to a body in a Bullet simulation. The force will only be applied for a single frame.

- `owner` → OP (read-only)
  Raw: The

- `rotate` → tdu.Vector
  Raw: Get or set the body's rotation in world space.

- `translate` → tdu.Position
  Raw: Get or set the body's translation in world space.

- `angularVelocity` → tdu.Vector
  Raw: Get or set the body's angular velocity.

- `linearVelocity` → tdu.Vector
  Raw: Get or set the body's linear velocity.

### Methods
- `applyImpulseForce(force, relPos=None)` → None
  Raw: Applies impulse force to a body in a Bullet simulation.

- `applyTorque(torque)` → None
  Raw: Applies torque to a body in a Bullet simulation. The torque will only be applied for a single frame.

- `applyImpulseTorque(torque)` → None
  Raw: Applies impulse torque to a body in a Bullet simulation.

- `applyForce(force, relPos=None)` → None
  Raw: Applies force to a body in a Bullet simulation. The force will only be applied for a single frame.

## BulletsolverCOMP_Class
Operator: BulletsolverCOMP (COMP)

### Members
- `actors` → list (read-only)
  Raw: Returns a list of all the

## ButtonCOMP_Class
Operator: ButtonCOMP (COMP)

### Methods
- `click(val, clickCount=1, force=False, left=False, middle=False, right=False)` → None
  Raw: Simulate a mouse click of a button panel.

## Camera_Class
Operator: Camera ()

### Members
- `dir` → tdu.Position
  Raw: Move the camera forward/back along in the ZX plane and rotate around its position.

- `pivot` → tdu.Position
  Raw: Get or set the 3D point in space where the camera will pivot around or towards.

- `position` → tdu.Position
  Raw: Get or set the 3D point in space where the camera is located.

### Methods
- `blendCamera(targetCamera, blendFactor)` → tdu.Camera
  Raw: Returns a camera that is blended with the given camera using the blendFactor. The camera position is blended using linear interpolation, while the rotation is blended using spherical linear interpolat

- `dolly()` → None
  Raw: Move the camera away or towards the pivot point.

- `frameBounds()` → float
  Raw: Set the camera to frame the given bounding box. Returns the width of the framed scene that can be used when setting up orthographic projections.

- `look()` → None
  Raw: Pivot the camera around its position.

- `move3D()` → None
  Raw: Move the camera using data from a 3D mouse.

- `pan()` → None
  Raw: Pan the camera in a 2D plane facing the pivot point.

- `setTransform()` → None
  Raw: Set the camera view matrix.

- `track()` → None
  Raw: Move the camera up/down in the Y-Axis or left/right.

- `transform()` → None
  Raw: Get the camera view matrix.

- `tumble()` → None
  Raw: Rotate the camera around the pivot point.

- `walk()` → None
  Raw: Move the camera forward/back along in the ZX plane and rotate around its position.

## CameraCOMP_Class
Operator: CameraCOMP (COMP)

### Methods
- `projectionInverse(x, y)` → tdu.Matrix
  Raw: Returns a dictionary of its transform and projection for use in a MPCDI definition file.

- `projection(x, y)` → tdu.Matrix
  Raw: Returns the projection matrix for the camera, given the X and Y aspect. In general these would be set to the width and height of your render.

- `mpcdiRegion(width, height)` → dict
  Raw: Returns a dictionary of its transform and projection for use in a MPCDI definition file.

## Cell_Class
Operator: Cell ()

### Members
- `valid` → bool (read-only)
  Raw: The cell offset to this cell by the specified amount, or None.

- `row` → int (read-only)
  Raw: The numeric row of the cell.

- `col` → int (read-only)
  Raw: The numeric column of the cell.

- `owner` → OP (read-only)
  Raw: The

- `val` → str
  Raw: Get or set the cell contents, which are always stored as a string value.

### Methods
- `run(*args, endFrame=False, fromOP=None, asParameter=False, group=None, delayFrames=0, delayMilliSeconds=0, wallTime=False, delayRef=None)` → td.Run
  Raw: The cell offset to this cell by the specified amount, or None.

## Channel_Class
Operator: Channel ()

### Members
- `valid` → bool (read-only)
  Raw: Copies the contents of the numpyArray into the Channel sample values.

- `index` → int (read-only)
  Raw: The numeric index of the channel.

- `name` → str (read-only)
  Raw: The name of the channel.

- `owner` → OP (read-only)
  Raw: The

- `exports` → list (read-only)
  Raw: The (possibly empty) list of

- `vals` → list
  Raw: Get or set the full list of

- `[index]` → float
  Raw: Sample values may be easily accessed from a Channel using the [] subscript operator.

### Methods
- `eval(index)` → float
  Raw: Evaluate the channel at the specified index sample index. If no index is given, the current index based on the current time is used.

- `evalFrame(frame)` → float
  Raw: Evaluate the channel at the specified frame. If no frame is given, the current frame is used.

- `evalSeconds(secs)` → float
  Raw: Evaluate the channel at the specified seconds. If no time is given, the current time is used.

- `numpyArray()` → numpy.ndarray
  Raw: Returns this channels data as a NumPy array with a length equal to the track length.

- `destroy()` → None
  Raw: Destroy and remove the actual Channel this object refers to. This operation is only valid when the channel belongs to a

- `average()` → float
  Raw: Returns the average value of all the channel samples.

- `min()` → float
  Raw: Returns the minimum value of all the channel samples.

- `max()` → float
  Raw: Returns the maximum value of all the channel samples.

- `copyNumpyArray(numpyArray)` → None
  Raw: Copies the contents of the numpyArray into the Channel sample values.

## ClipblenderCHOP_Class
Operator: ClipblenderCHOP (CHOP)

### Members
- `clipA` → OP (read-only)
  Raw: Trigger the next clip in sequence.

- `clipB` → OP (read-only)
  Raw: The clip being blended into.

- `currentClip` → OP (read-only)
  Raw: Clip A before the start of a transition, clip B otherwise.

- `currentFrame` → float (read-only)
  Raw: The frame index of the current clip output.

- `currentTime` → float (read-only)
  Raw: The time value of the current clip output.

- `endBlendTime` → float (read-only)
  Raw: The time at which the transition is complete.

- `isBlending` → bool (read-only)
  Raw: True if the output is transitioning between clips.

- `isQueued` → bool (read-only)
  Raw: True if transitions are delayed until current transition completes.

- `isTriggerWaiting` → bool (read-only)
  Raw: True if there is a pending triggered transition.

- `lastClipA` → float (read-only)
  Raw: Last value of clip A.

- `lastClipB` → float (read-only)
  Raw: Last value of clip B.

- `nextClip` → OP (read-only)
  Raw: The next clip to be sequenced.

- `startBlendTime` → float (read-only)
  Raw: The time the transition will start.

- `triggerClip` → OP (read-only)
  Raw: The clip .

### Methods
- `trigger(clipCHOP, waitEnd=False)` → None
  Raw: Trigger the next clip in sequence.

## ClockCHOP_Class
Operator: ClockCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: "Get a Timecode object representation of the hour, minute, second, and msec components."

## Color_Class
Operator: Color ()

### Members
- `r` → float
  Raw: Returns a new color that is a copy of the color.

- `g` → float
  Raw: Gets or sets the green component of the color.

- `b` → float
  Raw: Gets or sets the blue component of the color.

- `a` → float
  Raw: Gets or sets the alpha component of the color.

- `[index]` → float
  Raw: Sample values may be accessed from a Color using the [] subscript operator.

### Methods
- `copy()` → TDU.Color
  Raw: Returns a new color that is a copy of the color.

## Colors_Class
Operator: Colors ()

### Members
- `[&lt;color option name&gt;]` → triplet(r,g,b)
  Raw: Get or set specific color option, given a string key.

- `Iterator` → str
  Raw: Iterate over each color option name.

### Methods
- `resetToDefaults()` → None
  Raw: Iterate over each color option name.

- `len(Colors)` → int
  Raw: Returns the total color options.

## Connector_Class
Operator: Connector ()

### Members
- `index` → int (read-only)
  Raw: Disconnect this connector.

- `isInput` → bool (read-only)
  Raw: True when the connector is an input.

- `isOutput` → bool (read-only)
  Raw: True when the connector is an output.

- `inOP` → OP (read-only)
  Raw: Will return any input operators (e.g.

- `outOP` → OP (read-only)
  Raw: Will return any output operators (e.g.

- `owner` → OP (read-only)
  Raw: The

- `connections` → list (read-only)
  Raw: The list of

- `description` → str (read-only)
  Raw: A description for this connection. Example: 'Color Image'.

### Methods
- `connect(target)` → None
  Raw: Disconnect this connector.

## ContainerCOMP_Class
Operator: ContainerCOMP (COMP)

### Methods
- `click(u, v, clickCount=1, force=False, left=False, middle=False, right=False, group=None)` → None
  Raw: Simulate a mouse click of a sub-panel within a container panel. This can be used to click radio buttons within a single container.

- `clickChild(childIndex, clickCount=1, force=False, left=False, middle=False, right=False, group=None)` → None
  Raw: Simulate a mouse click of a sub-panel within a container panel. This can be used to click radio buttons within a single container.

## CopyCHOP_Class
Operator: CopyCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The current copy index, beginning at zero.

- `copyIndex` → int (read-only)
  Raw: The current copy index, beginning at zero.

## CopySOP_Class
Operator: CopySOP (SOP)

### Members
- `copyIndex` → int (read-only)
  Raw: The current

- `copyTotal` → int (read-only)
  Raw: The total number of copies.

- `inputPoint` → int (read-only)
  Raw: The current

## CUDAMemory_Class
Operator: CUDAMemory ()

### Members
- `ptr` → int (read-only)
  Raw: Returns the

- `size` → int (read-only)
  Raw: Returns the size of the CUDA Memory, in bytes.

- `shape` → CUDAMemoryShape (read-only)
  Raw: Returns the

## CUDAMemoryShape_Class
Operator: CUDAMemoryShape ()

### Members
- `width` → int
  Raw: Get/Set the data type of each color component, as a numpy data type. E.g numpy.uint8, numpy.float32. Note that for uint8 data types, the channel ordering will be BGRA for 4 component textures. It will

- `height` → int
  Raw: Get/Set the height in pixels of the memory.

- `numComps` → int
  Raw: Get/Set the number of color components per pixel of the memory.

- `depth` → int
  Raw: Get/Set the depth in pixels of the memory.

- `isCube` → bool
  Raw: Get/Set whether this shape describes a cubemap. Depth must equal 6.

- `is3D` → bool
  Raw: Get/Set whether this shape describes a 3D texture. If this is false and depth > 1, then it's a 2D array.

- `dataType` → numpy.dtype
  Raw: Get/Set the data type of each color component, as a numpy data type. E.g numpy.uint8, numpy.float32. Note that for uint8 data types, the channel ordering will be BGRA for 4 component textures. It will

## DattoCHOP_Class
Operator: DattoCHOP (CHOP)

### Members
- `inputCell` → Cell (read-only)
  Raw: The current input

- `inputCol` → int (read-only)
  Raw: The current input colunn being evaluated.

- `inputRow` → int (read-only)
  Raw: The current input row being evaluated.

- `inputTable` → OP (read-only)
  Raw: The current input

## DeleteSOP_Class
Operator: DeleteSOP (SOP)

### Members
- `inputPoint` → int (read-only)
  Raw: The current

- `inputPrim` → int (read-only)
  Raw: The current

## Dependency_Class
Operator: Dependency ()

### Members
- `val` → Any (read-only)
  Raw: Sets the value with optional information.

- `peekVal` → Any (read-only)
  Raw: This returns the same value as .val but does not create a dependency on the value.

- `callbacks` → list
  Raw: A modifiable list of functions. When the

- `ops` → list (read-only)
  Raw: A list of

- `listAttributes` → list (read-only)
  Raw: A list of

### Methods
- `modified()` → None
  Raw: This call is needed when changing not the value itself, but a subcomponent. For example, if

- `setVal(val, setInfo=None)` → None
  Raw: Sets the value with optional information.

## DmxinCHOP_Class
Operator: DmxinCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Get a Timecode object for the last ArtTimeCode packet received. See

## Dongle_Class
Operator: Dongle ()

### Members
- `serialNumber` → str (read-only)
  Raw: Returns a string which is the remote programming update context for the dongle.

### Methods
- `applyUpdate(str)` → None
  Raw: Takes an update as a string and applies it to the dongle.

- `createUpdateContext()` → str
  Raw: Returns a string which is the remote programming update context for the dongle.

## DongleList_Class
Operator: DongleList ()

### Methods
- `refreshDongles()` → None
  Raw: Returns True if the provided product code is installed on any of the connected dongles.

- `encrypt(firmCode, productCode, data)` → ByteArray
  Raw: Encrypts a string or byte array using a CodeMeter dongle with a given firm code and product code installed on it. If successful it returns a byte array with the encrypted data.

- `decrypt(firmCode, productCode, data)` → ByteArray
  Raw: Decrypts a string or byte array using a CodeMeter dongle with a given firm code and product code installed on it. If successful it returns a byte array with the decrypted data.

- `productCodeInstalled()` → bool
  Raw: Returns True if the provided product code is installed on any of the connected dongles.

## EngineCOMP_Class
Operator: EngineCOMP (COMP)

### Methods
- `goToDone()` → None
  Raw: Go to Done. Equivalent to pulsing the Go to Done parameter on the Engine Page.

## EvaluateDAT_Class
Operator: EvaluateDAT (DAT)

### Members
- `exprCol` → int (read-only)
  Raw: The input

- `exprRow` → int (read-only)
  Raw: The current expression row being evaluated.

- `exprTable` → DAT (read-only)
  Raw: The input

- `inputCell` → Cell (read-only)
  Raw: The current input

- `inputCol` → int (read-only)
  Raw: The current input column being evaluated.

- `inputRow` → int (read-only)
  Raw: The current input row being evaluated.

- `inputTable` → DAT (read-only)
  Raw: The input

## EventCHOP_Class
Operator: EventCHOP (CHOP)

### Methods
- `releaseEvent(id)` → int
  Raw: Manually create an event for this

- `createEvent(hold=False, samples=[], index=0, attackTime=None, attackLevel=None, decayTime=None, sustainTime=None, sustainMin=None, sustainMax=None, releaseTime=None, releaseLevel=None, speed=None)` → int
  Raw: Manually create an event for this

## FieldCOMP_Class
Operator: FieldCOMP (COMP)

### Methods
- `setKeyboardFocus(selectAll=False)` → None
  Raw: Set keyboard focus.

## FileinCHOP_Class
Operator: FileinCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

- `sampleIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## FileoutDAT_Class
Operator: FileoutDAT (DAT)

### Members
- `writeCount` → int (read-only)
  Raw: Used to write strings into the file.

### Methods
- `sendBytes(*messages)` → int
  Raw: Write one or more sequence of bytes. No terminators are appended.

- `send(*messages , terminator=' ')` → int
  Raw: Used to write strings into the file.

## FileoutPOP_Class
Operator: FileoutPOP (POP)

### Members
- `fileSuffix` → str (read-only)
  Raw: "Returns the generated file suffix. It will be generated based on the values of the parameters Unique Suffix and

## FlexsolverCOMP_Class
Operator: FlexsolverCOMP (COMP)

### Members
- `actors` → list (read-only)
  Raw: Returns a list of all the Actor COMPs in the Flex Solver simulation.

## GeometryCOMP_Class
Operator: GeometryCOMP (COMP)

### Methods
- `bounds(display=False, render=False, selected=False, recurse=True)` → Tuple[Tuple[float, float, float], Tuple[float, float, float], Tuple[float, float, float], Tuple[float, float, float]]
  Raw: Calculate the bounding box of the child geometry of this component.

## GeotextCOMP_Class
Operator: GeotextCOMP (COMP)

### Members
- `textHeight` → float (read-only)
  Raw: Returns a list of

- `textWidth` → float (read-only)
  Raw: The width of the text excluding padding. Only valid in Text mode

### Methods
- `evalTextSize(str)` → Tuple[float, float]
  Raw: Return a tuple with the width and height of the given string based on the current font settings in the Geo Text

- `layoutText(str)` → List
  Raw: Returns a list of

## GlslMAT_Class
Operator: GlslMAT (MAT)

### Members
- `compileResult` → str (read-only)
  Raw: The latest compile result.

## GlslmultiTOP_Class
Operator: GlslmultiTOP (TOP)

### Members
- `compileResult` → str (read-only)
  Raw: The latest compile result.

## GlslTOP_Class
Operator: GlslTOP (TOP)

### Members
- `compileResult` → str (read-only)
  Raw: The latest compile result.

## Group_Class
Operator: Group ()

### Members
- `default` → tuple (read-only)
  Raw: Destroys the current point/primitive group.

- `name` → str
  Raw: Set/gets the group name.

- `owner` → OP (read-only)
  Raw: Gets the owner of this group.

### Methods
- `destroy()` → None
  Raw: Destroys the current point/primitive group.

## GroupSOP_Class
Operator: GroupSOP (SOP)

### Members
- `inputPoint` → Point (read-only)
  Raw: The current

- `inputPrim` → Prim (read-only)
  Raw: The current

## InputPoint_Class
Operator: InputPoint ()

### Members
- `color` → TDU.Color (read-only)
  Raw: Get the barycentric coordinate of the geometry the inputPoint is a part of. This is faster than other methods to get the center of a

- `normal` → TDU.Vector (read-only)
  Raw: The normal for this point. This is different from the N attribute, since it can come from a

- `sopCenter` → TDU.Position (read-only)
  Raw: Get the barycentric coordinate of the geometry the inputPoint is a part of. This is faster than other methods to get the center of a

## InsertDAT_Class
Operator: InsertDAT (DAT)

### Members
- `subRow` → int (read-only)
  Raw: Current header name for Insert

- `subCol` → int (read-only)
  Raw: Current col index for Insert

- `fillName` → str (read-only)
  Raw: Current header name for Insert

## KinectazureTOP_Class
Operator: KinectazureTOP (TOP)

### Members
- `colorCameraIntrinsics` → Tuple[float, float, float, float, float, float, float, float, float, float, float, float, float, float] (read-only)
  Raw: Transforms a 3D world position to a u and v position in the depth camera image. The success member of the return value will be true if the position was transformed successfully. Returns a named tuple 

- `depthCameraIntrinsics` → Tuple[float, float, float, float, float, float, float, float, float, float, float, float, float, float] (read-only)
  Raw: A named tuple containing the intrinsic parameters of the depth camera:

### Methods
- `getRemapTransform(src, target)` → TDU.Matrix
  Raw: Returns a matrix that transforms between the coordinate spaces of the source and target devices.

- `transformWorldToColorImage(worldPos)` → Tuple[float, float, bool]
  Raw: Transforms a 3D world position to a u and v position in the color camera image. The success member of the return value will be true if the position was transformed successfully. Returns a named tuple 

- `transformWorldToDepthImage(worldPos)` → Tuple[float, float, bool]
  Raw: Transforms a 3D world position to a u and v position in the depth camera image. The success member of the return value will be true if the position was transformed successfully. Returns a named tuple 

## LagCHOP_Class
Operator: LagCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## License_Class
Operator: License ()

### Members
- `index` → int (read-only)
  Raw: Disable this license. Use with caution!

- `isEnabled` → bool (read-only)
  Raw: True if the license is locally enabled (That is, it has never been disabled).

- `isRemotelyDisabled` → bool (read-only)
  Raw: True if the license has been remotely disabled.

- `key` → str (read-only)
  Raw: The key sequence.

- `remoteDisableDate` → tuple[int, int, int] (read-only)
  Raw: The date the license was remotely disabled, expressed as a tuple (year, month, day).

- `status` → int (read-only)
  Raw: The numeric status code. Negative values indicate the license is not applicable to the current application. A value of zero indicates it does.

- `statusMessage` → str (read-only)
  Raw: A description of the status code.

- `systemCode` → str (read-only)
  Raw: The system code associated with this license.

- `type` → str (read-only)
  Raw: The license type, e.g. some products being 'Pro', 'Non-Commercial', 'Commercial'. See also app.product in

- `updateExpiryDate` → tuple[int, int, int] (read-only)
  Raw: The date updates for this license expires, expressed as a tuple (year, month, day).

- `version` → int (read-only)
  Raw: The numeric license version.

### Methods
- `serverDisable()` → None
  Raw: Disable this license. Use with caution!

## Licenses_Class
Operator: Licenses ()

### Members
- `disablePro` → bool (read-only)
  Raw: Install a

- `dongles` → list (read-only)
  Raw: Get the list of dongles connected to the system.

- `machine` → str (read-only)
  Raw: The computer machine name.

- `systemCode` → str (read-only)
  Raw: The unique computer system code.

- `isPro` → bool (read-only)
  Raw: When True, the application is running with a Pro license. It is recommended to use this and isNonCommerical over the type method.

- `isNonCommercial` → bool (read-only)
  Raw: When True, the application is running with a Non-Commercial license. It is recommended to use this and isPro over the type method.

- `type` → str (read-only)
  Raw: The highest ranking license type of all installed licenses, some products being 'Pro', 'Non-Commercial', 'Commercial'. See also app.product in

### Methods
- `install(key)` → bool
  Raw: Install a

## LightCOMP_Class
Operator: LightCOMP (COMP)

### Methods
- `projectionInverse(x, y)` → tdu.Matrix
  Raw: Returns a dictionary of its transform and projection for use in a MPCDI definition file.

- `projection(x, y)` → tdu.Matrix
  Raw: Returns the projection

- `mpcdiRegion(width, height)` → dict
  Raw: Returns a dictionary of its transform and projection for use in a MPCDI definition file.

## ListAttribute_Class
Operator: ListAttribute ()

### Members
- `text` → str (read-only)
  Raw: Returns true if the mouse is currently pressed over the cell/row/column/table.

- `textColor` → tuple[float, float, float, float]
  Raw: Get or set text color. In the form of a tuple (r, g, b, a).

- `textJustify` → JustifyType
  Raw: Get or set text justification. Value is one of: JustifyType.TOPLEFT, JustifyType.TOPCENTER, JustifyType.TOPRIGHT, JustifyType.CENTERLEFT, JustifyType.CENTER, JustifyType.CENTERRIGHT, JustifyType.BOTTO

- `textOffsetX` → float
  Raw: Get or set horizontal text offset.

- `textOffsetY` → float
  Raw: Get or set vertical text offset.

- `wordWrap` → bool
  Raw: Get or set word wrapping.

- `help` → str
  Raw: Get or set help string when rolling over the cell.

- `bgColor` → tuple[float, float, float, float]
  Raw: Get or set background color. In the form of a tuple (r, g, b, a).

- `fontFace` → str
  Raw: Get or set font face. Example 'verdana'.

- `fontFile` → str
  Raw: Get or set font file.

- `fontBold` → bool
  Raw: Get or set whether or not text is rendered in bold font.

- `fontItalic` → bool
  Raw: Get or set whether or not text is rendered italicized.

- `fontSizeX` → float
  Raw: Get or set font horizontal size.

- `fontSizeY` → float
  Raw: Get or set font vertical size. If not specified, uses fontSizeX.

- `sizeInPoints` → bool
  Raw: Get or set text size units. When True size is in points, when False it is in pixels.

- `top` → TOP
  Raw: Get or set background image

- `topFill` → FillMode
  Raw: Get or set background

- `topBorderInColor` → tuple[float, float, float, float]
  Raw: Get or set inside top color. In the form of a tuple (r, g, b, a).

- `topBorderOutColor` → tuple[float, float, float, float]
  Raw: Get or set outside top color. In the form of a tuple (r, g, b, a).

- `bottomBorderInColor` → tuple[float, float, float, float]
  Raw: Get or set inside bottom color. In the form of a tuple (r, g, b, a).

- `bottomBorderOutColor` → tuple[float, float, float, float]
  Raw: Get or set outside bottom color. In the form of a tuple (r, g, b, a).

- `leftBorderInColor` → tuple[float, float, float, float]
  Raw: Get or set inside left color. In the form of a tuple (r, g, b, a).

- `leftBorderOutColor` → tuple[float, float, float, float]
  Raw: Get or set outside left color. In the form of a tuple (r, g, b, a).

- `rightBorderInColor` → tuple[float, float, float, float]
  Raw: Get or set inside right color. In the form of a tuple (r, g, b, a).

- `rightBorderOutColor` → tuple[float, float, float, float]
  Raw: Get or set outside right color. In the form of a tuple (r, g, b, a).

- `colWidth` → float
  Raw: Get or set column width, expressed in pixels.

- `colStretch` → bool
  Raw: Get or set column stretchiness. When True, colWidth specifies minimum width.

- `rowHeight` → float
  Raw: Get or set row height, expressed in pixels.

- `rowStretch` → bool
  Raw: Get or set row stretchiness. When True, rowWidth specifies minimum width.

- `rowIndent` → float
  Raw: Get or set row indent, expressed in pixels.

- `editable` → bool
  Raw: Get or set whether or not contents are editable. When True, contents can be edited by clicking on the cell.

- `draggable` → bool
  Raw: Get or set whether or not cell is draggable.

- `focus` → bool (read-only)
  Raw: Returns True if the cell/row/column/table is currently being edited.

- `radio` → bool (read-only)
  Raw: Returns true if the mouse last selected the cell/row/column/table.

- `rollover` → bool (read-only)
  Raw: Returns true if the mouse is currently over the cell/row/column/table.

- `select` → bool (read-only)
  Raw: Returns true if the mouse is currently pressed over the cell/row/column/table.

## ListCOMP_Class
Operator: ListCOMP (COMP)

### Members
- `attribs` → td.ListAttribute (read-only)
  Raw: Return the actual row height including stretching.

- `colAttribs` → td.ListAttributes (read-only)
  Raw: The set of row

- `rowAttribs` → td.ListAttributes (read-only)
  Raw: The set of row

- `cellAttribs` → td.ListAttributes (read-only)
  Raw: The set of cell

- `displayAttribs` → td.ListAttributes (read-only)
  Raw: The set of attributes actually displayed in the cell. They are a combination of the cell/row/col/table attributes described above. Accessed by row and column. When combining attributes, cell attribute

- `focusCol` → int (read-only)
  Raw: Last column with focus for editing.

- `focusRow` → int (read-only)
  Raw: Last row with focus for editing.

- `radioCol` → int (read-only)
  Raw: The last selected column.

- `radioRow` → int (read-only)
  Raw: The last selected row.

- `rolloverCol` → int (read-only)
  Raw: The last column rolled over.

- `rolloverRow` → int (read-only)
  Raw: The last row rolled over.

- `selectCol` → int (read-only)
  Raw: The currently selected column.

- `selectRow` → int (read-only)
  Raw: The currently selected row.

- `selectionBorderColor` → Tuple[float, float, float, float]
  Raw: Get or set the border color for the separate selection, expressed as a 4-tuple (r,g,b,a), representing its red, green, blue and alpha value.

- `selectionColor` → Tuple[float, float, float, float]
  Raw: Get or set the background color for the separate selection, expressed as a 4-tuple (r,g,b,a), representing its red, green, blue and alpha value.

- `selections` → List[Tuple[int, int, int, int]]
  Raw: Get or set the row and column coordinates for separate selection formatting, expressed as a list of 4-tuples, each containing: (startrow, startcol, endrow, endcol).

- `dragRow` → int (read-only)
  Raw: The currently dragged row.

- `dragCol` → int (read-only)
  Raw: The currently dragged column.

- `dropRow` → int (read-only)
  Raw: The current row about to be dropped over.

- `dropCol` → int (read-only)
  Raw: The current col about to be dropped over.

### Methods
- `scroll(row, col)` → None
  Raw: Scroll List component to the row and column specified.

- `setKeyboardFocus(row, col, selectAll=False)` → None
  Raw: Selects and sets the keyboard focus in a cell of the table if the cell is a field.

- `reset()` → None
  Raw: Reset the list by running its initialize callbacks.

- `displlayColWidth()` → int
  Raw: Return the actual column width including stretching.

- `displayRowHeight()` → int
  Raw: Return the actual row height including stretching.

## LtcinCHOP_Class
Operator: LtcinCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Get a Timecode object for the timecode data representation of the LTC In

## LtcoutCHOP_Class
Operator: LtcoutCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Get a Timecode object for the timecode data representation of the LTC Out

## MathCHOP_Class
Operator: MathCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## Matrix_Class
Operator: Matrix ()

### Members
- `vals` → float (read-only)
  Raw: Multiplies the position by the matrix and returns the a new position as the result. If the matrix was not an transformation matrix, such as a projection matrix instead, the perspective divide by W wil

- `rows` → list[list] (read-only)
  Raw: The list of Matrix rows, each a list of values.

- `cols` → list[list] (read-only)
  Raw: The list of Matrix columns, each a list of values.

- `[row, column]` → float
  Raw: Gets or sets the specified entry in the matrix.

- `Matrix * Matrix` → TDU.Matrix
  Raw: Performs a matrix multiplication returns the results in a new matrix.

- `Matrix - Matrix` → TDU.Matrix
  Raw: Subtracts the matrices, component-by-component, and returns the results in a new matrix.

- `Matrix + Matrix` → TDU.Matrix
  Raw: Adds the matrices, component-by-component, and returns the results in a new matrix

### Methods
- `transpose()` → None
  Raw: Transpose the values in the matrix.

- `getTranspose()` → None
  Raw: Returns the transpose of the matrix, leaving the matrix itself unchanged.

- `invert()` → None
  Raw: Inverts the values in the matrix.

- `getInverse()` → TDU.Matrix
  Raw: Returns the inverse of the matrix, leaving the matrix itself unchanged.

- `determinant()` → float
  Raw: Returns the determinant of the matrix.

- `mapUnitSquareToQuad(blX, blY, brX, brY, tlX, tlY, trX, trY)` → None
  Raw: Set the matrix to be a projection matrix that maps coordinates from to a unit square (0,0) -> (1,1) space to a space defined by an arbitrary quadrilateral (blX, blY) -> (trX, trY). The 4 corners of th

- `mapQuadToUnitSquare(blX, blY, brX, brY, tlX, tlY, trX, trY)` → None
  Raw: Is the inverse of mapUnitSquareToQuad(). Mapping coordinates in an arbitrary quadrilateral into a space defined by the unit square.

- `fillTable(tableDAT)` → None
  Raw: Fill in the contents of a table from the matrix which the method is called upon.

- `numpyArray()` → numpy.ndarray
  Raw: Returns this matrix as a 4x4 NumPy array.

- `identity()` → None
  Raw: Replaces the values in the matrix with the

- `copy()` → TDU.Matrix
  Raw: Returns a new matrix that is a copy of the matrix.

- `translate(tx, ty, tz, fromRight=False)` → None
  Raw: Multiplies the current matrix by a new translation matrix created from tx, ty and tz. The translation is applied from the left of the matrix by default. That is to say, if T is the new translation mat

- `rotate(rx, ry, rz, fromRight=False, pivot=None)` → None
  Raw: Multiplies the current matrix by 3 rotation matrices, first a rotation around the X axis by rx degrees, followed by a rotation around the Y axis by ry degrees, followed by the same for rz. The rotatio

- `rotateOnAxis(rotationAxis, angle, fromRight=False, pivot=None)` → None
  Raw: Multiplies the current matrix by a new rotation matrix created by rotation angle degrees around the axis specified by rotationAxis. The angle is in degrees. The rotation is applied from the left of th

- `scale(sx, sy, sz, fromRight=False, pivot=None)` → None
  Raw: Multiplies the current matrix by a scale matrix created from sx, sy and sz. The scale is applied from the left of the matrix by default. That is to say, if S is the new scale matrix, and M is the curr

- `lookat(eyePos, target, up)` → None
  Raw: Multiplies the current matrix by a lookat matrix created using the given values to the matrix. The lookat matrix is applied from the left of the matrix by default. That is to say, if L is the new look

- `decompose()` → tuple[tuple, tuple, tuple]
  Raw: Decomposes the matrix into its scale, rotate and translate values. These are the same as the translate, rotate and scale that are in the

- `projectionFrustum(left, right, bottom, top, near, far)` → None
  Raw: Replaces the contents of the matrix with a projection matrix using the given frustum extents. The left, right, bottom, top extents are located on the near plane. The depth range generated by this matr

- `projectionFovX(fovX, aspectX, aspectY, near, far)` → None
  Raw: Replaces the contents of the matrix with a projection matrix defined by the FOV(given in degrees), an aspect ratio and near/far planes. The depth range generated by this matrix will be [0,1] from near

- `projectAndClip(pos : TDU.Position)` → TDU.Position | None
  Raw: Applies a projection matrix to a position if it's within the normalized clip space. Returns None if projected position is clipped.

- `projectionStereo(ipd : float, convergeZ : float, fovX : float, aspectX : float, aspectY : float, near : float, far : float, rightEye : bool=False)` → None
  Raw: Replaces the contents of the matrix with an asymetrical projection matrix suitable for stereo rendering. The left eye's projection matrix is given by default, set rightEye=True to get the right eye's 

## MediafileinfoDAT_Class
Operator: MediafileinfoDAT (DAT)

### Members
- `acodec` → str (read-only)
  Raw: Returns a MediaFileInfo named tuple.

- `acodecDesc` → str (read-only)
  Raw: Returns a descriptive audio codec of the media file.

- `bitdepth` → int (read-only)
  Raw: Returns the bit depth of the media file.

- `filename` → str (read-only)
  Raw: Returns the name of the media file.

- `filepath` → str (read-only)
  Raw: Returns the path of the media file.

- `fps` → float (read-only)
  Raw: Returns the

- `frames` → int (read-only)
  Raw: Returns the number of frames in the media file.

- `height` → int (read-only)
  Raw: Returns the height of the media file.

- `metadata` → dict (read-only)
  Raw: Returns dictionary containing all the metadata.

- `numChannels` → int (read-only)
  Raw: Returns the number of audio channels in the media file.

- `numSamples` → int (read-only)
  Raw: Returns the number of audio samples of the media file.

- `numSeconds` → float (read-only)
  Raw: Returns the length of the media file in seconds.

- `pixel_aspect_ratio` → float (read-only)
  Raw: Returns the pixel aspect ratio of the media file.

- `sampleRate` → int (read-only)
  Raw: Returns the audio sample rate of the media file.

- `valuesAreValid` → bool (read-only)
  Raw: Returns true if the values column have valid entries.

- `vcodec` → str (read-only)
  Raw: Returns the video codec of the media file.

- `vcodecDesc` → str (read-only)
  Raw: Returns a descriptive video codec of the media file.

- `width` → int (read-only)
  Raw: Returns the width of the media file.

### Methods
- `fetchInfo(filename)` → MediaFileInfo
  Raw: Returns a MediaFileInfo named tuple.

## Mesh_Class
Operator: Mesh ()

### Members
- `closedU` → bool (read-only)
  Raw: Iterate over each vertex.

- `closedV` → bool (read-only)
  Raw: Returns True if the mesh is closed in V, False otherwise.

- `numRows` → int (read-only)
  Raw: Number of rows in the mesh.

- `numCols` → int (read-only)
  Raw: Number of columns in the mesh.

- `center` → TDU.Position
  Raw: Get or set the barycentric coordinate of this primitive. It is expressed as a TDU.Position object.

- `index` → int (read-only)
  Raw: The primitive index in the list.

- `normal` → TDU.Vector (read-only)
  Raw: The calculated normal vector of this primitive, expressed as a TDU.Vector object.

- `owner` → OP (read-only)
  Raw: The

- `weight` → float (read-only)
  Raw: The associated weight of the primitive. Only certain primitives, such as those created by the

- `direction` → TDU.Vector (read-only)
  Raw: A normalized vector pointing from the centroid of the

- `min` → TDU.Position (read-only)
  Raw: The minimum coordinates of this primitive along each dimension, expressed as a TDU.Position object.

- `max` → TDU.Position (read-only)
  Raw: The maximum coordinates of this primitive along each dimension, expressed as a TDU.Position object.

- `size` → TDU.Position (read-only)
  Raw: The size of this primitive along each dimension, expressed as a TDU.Position object.

- `[index]` → td.Vertex
  Raw: Get specific vertex given an integer index

- `Iterator` → td.Vertex
  Raw: Iterate over each vertex.

### Methods
- `destroy(destroyPoints=True)` → None
  Raw: Destroy and remove the actual primitive this object refers to. This operation is only valid when the primitive belongs to a

- `eval(u, v)` → TDU.Position
  Raw: Evaluate the

- `len(Prim)` → int
  Raw: Returns the total number of vertices.

## MidiinCHOP_Class
Operator: MidiinCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Get a Timecode object representation of the last set of quarter frame messages. See

## MidioutCHOP_Class
Operator: MidioutCHOP (CHOP)

### Methods
- `send(*messages)` → None
  Raw: Sends the timecode to the device as a full MTC message.

- `sendBalance(channel, value)` → None
  Raw: Sends a Balance event through the

- `sendNoteOn(channel, index, value)` → None
  Raw: Sends a Note On event through the

- `sendPolyKeyPressure(channel, index, value)` → None
  Raw: Sends a Polyphonic Key Pressure event through the

- `sendPitchBend(channel, value)` → None
  Raw: Sends a Pitch Bend event through the

- `sendEffectsDepth(channel, index,value)` → None
  Raw: Sends a Effects Depth event through the

- `sendMonoOn(channel,value)` → None
  Raw: Sends a Mono On/Poly Off event through the

- `panic()` → None
  Raw: Sends a volume off event for each channel and note off event for each note.

- `sendDamperPedal(channel,value)` → None
  Raw: Sends a Damper Pedal event through the

- `sendChannelPressure(channel, value)` → None
  Raw: Sends a Channel Pressure event through the

- `sendControl(channel, index, value)` → None
  Raw: Sends a Controller event through the

- `sendDataDecrement(channel,value)` → None
  Raw: Sends a Data Decrement event through the

- `sendAllNotesOff(channel,value)` → None
  Raw: Sends a All Notes Off event through the

- `sendSoundController(channel, index,value)` → None
  Raw: Sends a Sound Controller event through the

- `sendBankSelect(channel,value)` → None
  Raw: Sends a Bank Select event through the

- `sendMainVolume(channel, value)` → None
  Raw: Sends a Main Volume event through the

- `sendSoftPedal(channel,value)` → None
  Raw: Sends a Soft Pedal event through the

- `sendLocalControl(channel,value)` → None
  Raw: Sends a Local Control Controllers event through the

- `sendPolyOn(channel,value)` → None
  Raw: Sends a Poly On/Mono Off event through the

- `sendPan(channel, value)` → None
  Raw: Sends a Pan event through the

- `sendResetAllControllers(channel,value)` → None
  Raw: Sends a Reset All Controllers event through the

- `sendBreathController(channel, value)` → None
  Raw: Sends a Breath Controller event through the

- `sendPortamentoTime(channel, value)` → None
  Raw: Sends a Portamento Time event through the

- `sendDataIncrement(channel,value)` → None
  Raw: Sends a Data Increment event through the

- `sendFootController(channel, value)` → None
  Raw: Sends a Foot Controller event through the

- `sendHold2(channel,value)` → None
  Raw: Sends a Hold2 event through the

- `sendSostenuto(channel,value)` → None
  Raw: Sends a Sostenuto event through the

- `sendOmniOff(channel,value)` → None
  Raw: Sends a Omni Off event through the

- `sendGeneralPurposeController(channel, index,value)` → None
  Raw: Sends a General Purpose Controller event through the

- `sendNoteOff(channel, index, value)` → None
  Raw: Sends a Note Off event through the

- `sendExclusive(*messages)` → None
  Raw: Send a sytem exclusive message through this

- `sendPortamento(channel,value)` → None
  Raw: Sends a Portamento event through the

- `sendEffectControl(channel, index,value)` → None
  Raw: Sends a Main Volume event through the

- `sendLegatoFootswitch(channel,value)` → None
  Raw: Sends a Legato Footswitch event through the

- `sendModulationWheel(channel,value)` → None
  Raw: Sends a Modulation Wheel event through the

- `sendProgram(channel, value)` → None
  Raw: Sends a Program Change event through the

- `sendOmniOn(channel,value)` → None
  Raw: Sends a Omni On event through the

- `sendPortamentoControl(channel,value)` → None
  Raw: Sends a Portamento Control event through the

- `sendTimecode(timecode)` → None
  Raw: Sends the timecode to the device as a full MTC message.

## ModelSOP_Class
Operator: ModelSOP (SOP)

### Methods
- `appendPoint()` → Point
  Raw: Copy geometry from the specified

- `appendPoly(numVertices, closed=True, addPoints=True)` → Poly
  Raw: Append a

- `appendBezier(numVertices, closed=False, order=4, addPoints=True)` → Bezier
  Raw: Append a

- `appendMesh(numRows, numCols, closedU=False, closedV=False, addPoints=True)` → Mesh
  Raw: Append a

- `clear()` → None
  Raw: Remove all geometry.

- `copy(sop)` → None
  Raw: Copy geometry from the specified

## Monitor_Class
Operator: Monitor ()

### Members
- `index` → int (read-only)
  Raw: The refresh rate the monitor is currently running at.

- `isPrimary` → bool (read-only)
  Raw: Returns true, if this monitor is the primary display.

- `isAffinity` → bool (read-only)
  Raw: Returns true, if this monitor is connected to the

- `connectedGPUName` → str (read-only)
  Raw: The name associated with this monitor's

- `connectedGPUIndex` → int (read-only)
  Raw: The index of this monitor's

- `width` → int (read-only)
  Raw: The width of the monitor area, measured in pixels.

- `height` → int (read-only)
  Raw: The height of the monitor area, measured in pixels.

- `left` → int (read-only)
  Raw: The position of left edge of the monitor area, measured in pixels.

- `right` → int (read-only)
  Raw: The position of right edge of the monitor area, measured in pixels.

- `top` → int (read-only)
  Raw: The position of top edge of the monitor area, measured in pixels.

- `bottom` → int (read-only)
  Raw: The position of bottom edge of the monitor area, measured in pixels.

- `displayName` → str (read-only)
  Raw: The unique display name associated with this monitor.

- `description` → str (read-only)
  Raw: A description of the monitor or its display adapter.

- `dpiScale` → float (read-only)
  Raw: The DPI Scaling factor the monitor is current set to.

- `scaledWidth` → int (read-only)
  Raw: The width of the monitor area, measured in points.

- `scaledHeight` → int (read-only)
  Raw: The height of the monitor area, measured in points.

- `scaledLeft` → int (read-only)
  Raw: The position of left edge of the monitor area, measured in points.

- `scaledRight` → int (read-only)
  Raw: The position of right edge of the monitor area, measured in points.

- `scaledTop` → int (read-only)
  Raw: The position of top edge of the monitor area, measured in points.

- `scaledBottom` → int (read-only)
  Raw: The position of bottom edge of the monitor area, measured in points.

- `serialNumber` → str (read-only)
  Raw: The serial number name associated with this monitor. May be blank.

- `refreshRate` → float (read-only)
  Raw: The refresh rate the monitor is currently running at.

## Monitors_Class
Operator: Monitors ()

### Members
- `primary` → int (read-only)
  Raw: Causes the application to behave as if a monitor device has changed.

- `width` → int (read-only)
  Raw: The width of the combined monitor area, measured in pixels.

- `height` → int (read-only)
  Raw: The height of the combined monitor area, measured in pixels.

- `left` → int (read-only)
  Raw: The leftmost edge of the combined monitor area, measured in pixels.

- `right` → int (read-only)
  Raw: The rightmost edge of the combined monitor area, measured in pixels.

- `top` → int (read-only)
  Raw: The topmost position of the combined monitor area, measured in pixels.

- `bottom` → int (read-only)
  Raw: The bottommost position of the combined monitor area, measured in pixels.

### Methods
- `locate(x,y)` → td.Monitor
  Raw: Return the

- `refresh()` → None
  Raw: Causes the application to behave as if a monitor device has changed.

## MoviefileinTOP_Class
Operator: MoviefileinTOP (TOP)

### Members
- `fileStartTimecode` → Timecode (read-only)
  Raw: Preloads the movie by opening it and pre-reading the first frames. Use the isFullyPreRead member to see if it's ready to play. This is done in a way to minimize impact on the existing playback of the 

- `metadata` → dict (read-only)
  Raw: Dictionary containing all the metadata.

- `fileHeight` → int (read-only)
  Raw: Height of the movie, in pixels.

- `fileWidth` → int (read-only)
  Raw: Width of the movie, in pixels.

- `hasAudio` → bool (read-only)
  Raw: True if the movie contains audio.

- `hasDecodeErrors` → bool (read-only)
  Raw: True if any frames failed to decode, likely due to file corruption. Currently only works on Hap codec files.

- `index` → float (read-only)
  Raw: Current movie index.

- `indexFraction` → float (read-only)
  Raw: Current movie index expressed as a fraction of total length.

- `isFullyPreRead` → bool (read-only)
  Raw: True if the movie has pre-read all of its Pre-Read frames and is ready to play. For single images this is true when the image is ready to be shown. When a movie is playing, this member will flip betwe

- `isInvalid` → bool (read-only)
  Raw: True if the movie has failed to load.

- `isLastFrame` → bool (read-only)
  Raw: True if the movie is currently showing its last frame.

- `isLoopFrame` → bool (read-only)
  Raw: True if the movie has just looped and is showing the first frame after a loop.

- `isOddField` → bool (read-only)
  Raw: True if the current displayed de-interlaced frame is the odd field.

- `isOpen` → bool (read-only)
  Raw: True if the file has been opened.

- `isOpening` → bool (read-only)
  Raw: True when the file is opening.

- `isPreloading` → bool (read-only)
  Raw: True when the file is preloading.

- `lastIndexUploaded` → float (read-only)
  Raw: The index of the if the last frame uploaded to the

- `numHeaders` → int (read-only)
  Raw: Returns the number of key-value pairs in the file's header.

- `numImages` → float (read-only)
  Raw: The number of images in the movie.

- `numSeconds` → float (read-only)
  Raw: The number of seconds in the movie.

- `rate` → float (read-only)
  Raw: The movie sample rate.

- `sourceChannels` → tuple (read-only)
  Raw: A list of the available channels in the file e.g. R, G, B, A for typical color images.

- `start` → float (read-only)
  Raw: The start index of the movie.

- `trueIndex` → float (read-only)
  Raw: The actual current index of the movie, disregarding trimming and other options.

- `trueNumImages` → float (read-only)
  Raw: The actual number of images contained in the movie, not affected by trimming.

- `timecode` → tdu.Timecode (read-only)
  Raw: Get a Timecode object for the timecode data representation of the true current index of the movie. See

### Methods
- `findHeader(key)` → str
  Raw: Returns the value of the header with the given key. This method will return a blank string if the header is not found.

- `getHeader(index)` → tuple
  Raw: Returns a tuple with the key and value of the header at the given index. This method will throw an error if the index is not valid. The numHeaders member can be used to get the number of valid headers

- `unload(cacheMemory=False)` → None
  Raw: Unloads the movie and frees its memory usage. The movie will open again next time it cooks, so make sure nothing is still using it to keep it closed.

- `preload(index)` → None
  Raw: Preloads the movie by opening it and pre-reading the first frames. Use the isFullyPreRead member to see if it's ready to play. This is done in a way to minimize impact on the existing playback of the 

## MoviefileoutTOP_Class
Operator: MoviefileoutTOP (TOP)

### Members
- `fileSuffix` → str (read-only)
  Raw: Returns the generated file suffix. It will be generated based on the values of the parameters Unique Suffix and N, plus the file extension. It will take one of two forms:

## MpcdiTOP_Class
Operator: MpcdiTOP (TOP)

### Methods
- `cameraTransform(bufferId=None, regionId=None)` → tdu.Matrix
  Raw: Gets the loaded projection matrix for the configuration. This should be referenced in the 'Proj Matrix/

- `projection(bufferId=None, regionId=None)` → tdu.Matrix
  Raw: Gets the loaded projection matrix for the configuration. This should be referenced in the 'Proj Matrix/

## MqttclientDAT_Class
Operator: MqttclientDAT (DAT)

### Members
- `isConnected` → bool (read-only)
  Raw: Unsubscribe from a previously subscribed topic.

### Methods
- `publish(topic, payload, qos=0, retain=False)` → None
  Raw: Publish content to a specific topic.

- `subscribe(topic, qos=0)` → None
  Raw: Subscribe to a specific topic from the broker.

- `unsubscribe(topic)` → None
  Raw: Unsubscribe from a previously subscribed topic.

## NetworkEditor_Class
Operator: NetworkEditor ()

### Members
- `showBackdropCHOPs` → bool (read-only)
  Raw: Detach the pane into a floating window. Returns True if successful.

- `showBackdropGeometry` → bool
  Raw: Enable or disable

- `showBackdropTOPs` → bool
  Raw: Enable or disable

- `showColorPalette` → bool
  Raw: Enable or disable display of the operator color palette selector.

- `showDataLinks` → bool
  Raw: Enable or disable operator data links.

- `showList` → bool
  Raw: Control display of operators as a list, or connected nodes.

- `showNetworkOverview` → bool
  Raw: Enable or disable display of the network overview.

- `showParameters` → bool
  Raw: Enable or disable display of the currently selected operator parameters.

- `straightLinks` → bool
  Raw: Control display of operator links as straight or curved.

- `x` → float
  Raw: Get or set the x coordinate of the network editor area, where 1 unit = 1 pixel when zoom = 1.

- `y` → float
  Raw: Get or set the y coordinate of the network editor area, where 1 unit = 1 pixel when zoom = 1.

- `zoom` → float
  Raw: Get or set the zoom factor of the network editor area, where a zoom factor of 1 draws each node at its unscaled resolution.

- `owner` → COMP
  Raw: Get or set the

- `id` → int (read-only)
  Raw: A unique numeric identifier.

- `link` → int
  Raw: Get or set the numeric link index.

- `enable` → bool
  Raw: Get or set mouse and keyboard interactivity on the pane.

- `maximize` → bool
  Raw: Enable or disable the pane maximize state.

- `name` → str
  Raw: Get or set the pane name.

- `ratio` → float
  Raw: Get or set the split proportion of the pane, if the pane was previously split.

- `bottomLeft` → Coords (read-only)
  Raw: The coordinates of the bottom left corner, expressed as both x/y and u/v in a named tuple.

- `topRight` → Coords (read-only)
  Raw: The coordinates of the top right corner, expressed as both x/y and u/v in a named tuple.

- `type` → PaneType (read-only)
  Raw: The enumerated type of the pane. Example: NetworkEditor.

### Methods
- `fitWidth(width)` → None
  Raw: Fit the network area to specified width, specified in node units. This affects the zoom factor.

- `fitHeight(height)` → None
  Raw: Fit the network area to specified height, specified in node units. This affects the zoom factor.

- `home(zoom=True, op=None)` → None
  Raw: Home all operators in the network.

- `homeSelected(zoom=True)` → None
  Raw: Home all selected operators in the network.

- `placeOPs(listOfOPs, inputIndex=None, outputIndex=None, delOP=None, undoName='Operators')` → None
  Raw: Use the mouse to place the specified operators in the pane.

- `changeType(paneType)` → td.Pane
  Raw: Change the pane to the specified type. Will return a new Pane object that represents the

- `close()` → None
  Raw: Close the pane.

- `floatingCopy()` → td.Pane
  Raw: Return a floating copy of the pane.

- `splitBottom()` → td.Pane
  Raw: Split the bottom portion of the pane into a new pane.

- `splitLeft()` → td.Pane
  Raw: Split the left portion of the pane into a new pane.

- `splitRight()` → td.Pane
  Raw: Split the right portion of the pane into a new pane.

- `splitTop()` → td.Pane
  Raw: Split the top portion of the pane into a new pane.

- `tearAway()` → bool
  Raw: Detach the pane into a floating window. Returns True if successful.

## NoiseCHOP_Class
Operator: NoiseCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## NotchTOP_Class
Operator: NotchTOP (TOP)

### Members
- `exposedProperties` → dict (read-only)
  Raw: Return a dictionary of key/value pairs representing all the exposed properties imported from the Notch block. The key is the UUID and the value is the corresponding

## NvidiaflexsolverCOMP_Class
Operator: NvidiaflexsolverCOMP (COMP)

### Members
- `actors` → list (read-only)
  Raw: Returns a list of all the Actor COMPs in the Flex Solver simulation.

## OakdeviceCHOP_Class
Operator: OakdeviceCHOP (CHOP)

### Members
- `masterFrame` → float (read-only)
  Raw: Unlock the depthai device of the OAK Device

- `masterSample` → float (read-only)
  Raw: Get the master time index in samples.

- `masterSeconds` → float (read-only)
  Raw: Get the master time index in seconds.

- `masterTimecode` → str (read-only)
  Raw: Get the master time index as a timecode in the form 00:00:00.00.

- `runningFrame` → float (read-only)
  Raw: Get or set the running time index in frames.

- `runningSample` → float (read-only)
  Raw: Get or set the running time index in samples.

- `runningSeconds` → float (read-only)
  Raw: Get or set the running time index in seconds.

- `runningTimecode` → str (read-only)
  Raw: Get or set the running time index as a timecode in the form 00:00:00.00.

- `timecode` → tdu.Timecode (read-only)
  Raw: Get the master timecode.

### Methods
- `lockDevice()` → 'depthai.Device'
  Raw: Get the depthai

- `sendMessage(message, streamName: str)` → None
  Raw: Sends a message to a stream owned by the OAK Device

- `unlockDevice()` → None
  Raw: Unlock the depthai device of the OAK Device

## OakselectCHOP_Class
Operator: OakselectCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Reorder custom parameter pages.

### Methods
- `appendChan(name)` → Channel
  Raw: Append a new channel to the

- `appendCustomPage(name)` → Page
  Raw: Add a new page of custom parameters. See Page Class for more details.

- `clear()` → None
  Raw: Remove all channels from the

- `copy(chop)` → None
  Raw: Match all of this CHOPs channel data to the given

- `copyNumpyArray(numpyArray, baseName='chan')` → None
  Raw: Copies the contents of the numpyArray into the

- `destroyCustomPars()` → int
  Raw: Remove all custom parameters from the OAK Select

- `sortCustomPages(*pages)` → None
  Raw: Reorder custom parameter pages.

## OakselectTOP_Class
Operator: OakselectTOP (TOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Get the master timecode from the referenced OAK Device

## OpenvrCHOP_Class
Operator: OpenvrCHOP (CHOP)

### Methods
- `triggerHapticPulse(controllerIndex, analogIndex, durationMilliseconds)` → None
  Raw: Triggers a haptic/vibration pulse on the given controller/analog index for the specified number of milliseconds.

## OpfindDAT_Class
Operator: OpfindDAT (DAT)

### Callbacks
- `onInitGetColumnNames(dat)`
- `onFindOPGetValues(dat, curOp, row)`
- `onFindOPGetInclude(dat, curOp, row)`

## Options_Class
Operator: Options ()

### Members
- `[&lt;option name&gt;]` → value
  Raw: Get or set specific option given an option name key.

- `Iterator` → str
  Raw: Iterate over each option name.

### Methods
- `resetToDefaults()` → None
  Raw: Iterate over each option name.

- `len(Options)` → int
  Raw: Returns the total number of options.

## OpviewerCOMP_Class
Operator: OpviewerCOMP (COMP)

### Methods
- `isViewable(path)` → bool
  Raw: Returns true if this operator can view the specified operator without recursion issues."

## OscinDAT_Class
Operator: OscinDAT (DAT)

### Methods
- `sendBytes(*messages)` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

- `sendOSC(address, *values, asBundle=False, useNonStandardTypes=True, use64BitPrecision=False)` → int
  Raw: Send an OSC formatted message through this connection. Multiple pairs of address/valueLists arguments to be passed. This method will automatically decide what OSC type to convert the passed values to.

- `send(*messages, terminator='')` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

## OscoutDAT_Class
Operator: OscoutDAT (DAT)

### Methods
- `sendBytes(*messages)` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

- `sendOSC(address, *values, asBundle=False, useNonStandardTypes=True, use64BitPrecision=False)` → int
  Raw: Send an OSC formatted message through this connection. Multiple pairs of address/valueLists arguments to be passed. This method will automatically decide what OSC type to convert the passed values to.

- `send(*messages, terminator='')` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

## Page_Class
Operator: Page ()

### Members
- `name` → bool (read-only)
  Raw: Resets all the parameters in the page.

- `owner` → OP (read-only)
  Raw: The

- `parGroups` → list (read-only)
  Raw: A list of

- `pars` → list (read-only)
  Raw: The list of

- `index` → int (read-only)
  Raw: The numeric index of this page.

- `isCustom` → bool (read-only)
  Raw: Boolean for whether this page is custom or not.

### Methods
- `appendOP(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a node reference type

- `appendCOMP(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a

- `appendObject(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a

- `appendPanelCOMP(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a Panel

- `appendTOP(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a

- `appendCHOP(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a

- `appendSOP(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a

- `appendPOP(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a

- `appendMAT(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a

- `appendDAT(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a

- `appendInt(name, label=None, size=1, order=None, replace=True)` → ParGroup
  Raw: Create a integer type

- `appendFloat(name, label=None, size=1, order=None, replace=True)` → ParGroup
  Raw: Create a float type

- `appendXY(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a XY position type

- `appendXYZ(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a XYZ position type

- `appendXYZW(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a XYZW position type

- `appendWH(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a WH size type

- `appendUV(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a UV 2D texture type

- `appendUVW(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a UVW 3D texture type

- `appendRGB(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a RGB color type

- `appendRGBA(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a RGBA color type

- `appendStr(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a string type

- `appendStrMenu(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a menu type

- `appendMenu(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a menu type

- `appendFile(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a file reference type

- `appendFileSave(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a file save reference type parameter. Has built-in functionality to open a new file picker window.

- `appendFolder(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a folder reference type

- `appendPulse(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a pulse button type

- `appendMomentary(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a momentary button type

- `appendToggle(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a toggle button type

- `appendPython(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a python expression

- `appendParGroup(name, parGroup=None, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a parGroup with attributes copied from an existing parameter. Returns the created

- `appendHeader(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a header parameter. Returns the created

- `appendSequence(name, label=None, order=None, replace=True)` → ParGroup
  Raw: Create a Sequence header parameter. Returns the created

- `destroy()` → None
  Raw: Destroy the page this object refers to, and all its parameters.

- `sort(*pars)` → None
  Raw: Reorder custom parameter groups or parameters in specified order.

- `resetPars()` → bool
  Raw: Resets all the parameters in the page.

## Pane_Class
Operator: Pane ()

### Members
- `owner` → COMP (read-only)
  Raw: Detach the pane into a floating window. Returns True if successful.

- `id` → int (read-only)
  Raw: A unique numeric identifier.

- `link` → int
  Raw: Get or set the numeric link index.

- `enable` → bool
  Raw: Get or set mouse and keyboard interactivity on the pane.

- `maximize` → bool
  Raw: Enable or disable the pane maximize state.

- `name` → str
  Raw: Get or set the pane name.

- `ratio` → float
  Raw: Get or set the split proportion of the pane, if the pane was previously split.

- `bottomLeft` → Coords (read-only)
  Raw: The coordinates of the bottom left corner, expressed as both x/y and u/v in a named tuple.

- `topRight` → Coords (read-only)
  Raw: The coordinates of the top right corner, expressed as both x/y and u/v in a named tuple.

- `open` → bool (read-only)
  Raw: Returns True if the Pane is currently open.

- `type` → PaneType (read-only)
  Raw: The enumerated type of the pane. Example: NetworkEditor.

### Methods
- `changeType(paneType)` → td.Pane
  Raw: Change the pane to the specified type. Will return a new Pane object that represents the

- `close()` → None
  Raw: Close the pane.

- `floatingCopy()` → td.Pane
  Raw: Return a floating copy of the pane.

- `splitBottom()` → td.Pane
  Raw: Split the bottom portion of the pane into a new pane.

- `splitLeft()` → td.Pane
  Raw: Split the left portion of the pane into a new pane.

- `splitRight()` → td.Pane
  Raw: Split the right portion of the pane into a new pane.

- `splitTop()` → td.Pane
  Raw: Split the top portion of the pane into a new pane.

- `tearAway()` → bool
  Raw: Detach the pane into a floating window. Returns True if successful.

## Panel_Class
Operator: Panel ()

### Members
- `owner` → OP (read-only)
  Raw: The

## PanelValue_Class
Operator: PanelValue ()

### Members
- `name` → str (read-only)
  Raw: True if the referenced panel value currently exists, False if it has been deleted.

- `owner` → OP (read-only)
  Raw: The

- `val` → Any
  Raw: Get or set the panel value.

- `valid` → bool (read-only)
  Raw: True if the referenced panel value currently exists, False if it has been deleted.

## Panes_Class
Operator: Panes ()

### Members
- `current` → td.Pane (read-only)
  Raw: Iterate over each pane.

- `[index]` → td.Pane
  Raw: Get specific pane, referenced by string or index.

- `Iterator` → td.Pane
  Raw: Iterate over each pane.

### Methods
- `createFloating(type=None, name=None, maxWidth=1920, maxHeight=1080, monitorSpanWidth=0.9, monitorSpanHeight=0.9)` → Pane
  Raw: Return a floating pane.

- `len(Panes)` → int
  Raw: Returns the total number of panes.

## Par_Class
Operator: Par ()

### Members
- `owner` → OP (read-only)
  Raw: True if the provided Par is the same parameter on the same operator. Because

- `val` → Any
  Raw: Get or set the

- `expr` → str
  Raw: Get or set the non-evaluated expression only. To get the parameter's current value, regardless of the

- `enable` → bool
  Raw: Get or set the parameter's enable state. Can only be set on

- `enableExpr` → str
  Raw: Get or set an expression that controls the enable state for this parameter. This member is shared by all parameters and the parameter group.

- `readOnly` → bool
  Raw: Get or set the parameter's read only status. When

- `bindExpr` → str
  Raw: Get or set an expression that returns a bindable object. This can be used to bind this parameter's constant value to the referenced object's value.

- `bindMaster` → OP (read-only)
  Raw: The object to which this parameter is bound to, possibly None.

- `bindReferences` → list (read-only)
  Raw: The (possibly empty) list of objects which bind to this parameter.

- `bindRange` → bool
  Raw: Get or set parameter's range binding state. If True, min, max, clampMin, clampMax, normMin, normMax, normVal values will be based on master bind parameter. Can only be set on Custom Parameters.

- `vecIndex` → int (read-only)
  Raw: The parameter's vector index. For example,

- `name` → str
  Raw: Get or set the parameter's unique name.

- `label` → str
  Raw: Get or set the parameter's label.

- `subLabel` → str (read-only)
  Raw: Returns the name of the sub-label.

- `startSection` → bool
  Raw: Get or set the parameter's separator status. When

- `help` → str (read-only)
  Raw: Get or set a custom parameter's help text. To see any parameter's help, rollover the parameter while holding the Alt key. For built-in parameters this can be used to get the parameter's help text.

- `parGroup` → ParGroup (read-only)
  Raw: The

- `min` → int
  Raw: Get or set the parameter's numerical minimum value. The parameter's value will be clamped at that minimum if clampMin = True. Can only be set on

- `max` → int
  Raw: Get or set the parameter's numerical maximum value. The parameter's value will be clamped at that maximum if clampMax = True. Can only be set on

- `clampMin` → bool
  Raw: Get or set the parameter's numerical clamping behavior. If set to clampMin = True, the parameter will clamp on the lower end at the value specified in min Can only be set on

- `clampMax` → bool
  Raw: Get or set the parameter's numerical clamping behavior. If set to clampMax = True, the parameter will clamp on the upper end at the value specified in max Can only be set on

- `normMin` → int
  Raw: Get or set the parameter's minimum slider value if the parameter is a numerical slider. Can only be set on

- `normMax` → int
  Raw: Get or set the parameter's maximum slider value if the parameter is a numerical slider. Can only be set on

- `normVal` → float
  Raw: Get or set the parameter's value as a normalized slider position. Can only be set on

- `default` → Any
  Raw: Get or set the parameter's default value. Can only be set on

- `defaultBindExpr` → str
  Raw: Get or set the parameter's default bind expression. Can only be set on Custom Parameters.

- `defaultExpr` → str
  Raw: Get or set the parameter's default expression. Can only be set on

- `defaultMode` → ParMode
  Raw: Get or set the parameter's default evaluation mode.

- `order` → float
  Raw: Get or set the parameter's position on the parameter page. Can only be set on

- `page` → td.Page
  Raw: Get or set the parameter page the custom parameter is part of. Can only be set on

- `sequence` → Sequence | None (read-only)
  Raw: The

- `sequenceBlock` → SequenceBlock | None (read-only)
  Raw: The

- `password` → bool
  Raw: Get or set the parameter's password mode. When True all text is rendered as asterisks. Can only be set on Custom string, int or float parameters.

- `mode` → ParMode
  Raw: Get or set the parameter's evaluation mode.

- `prevMode` → ParMode (read-only)
  Raw: The parameter's previous evaluation mode.

- `menuNames` → list
  Raw: Get or set a list of all possible menu choice names. In the case of non menu parameters, None is returned. Can only be set on

- `menuLabels` → list
  Raw: Get or set a list of all possible menu choice labels. In the case of non menu parameters, None is returned. Can only be set on

- `menuIndex` → int
  Raw: Get or set a menu constant value by its index.

- `menuSource` → str
  Raw: Get or set an expression that returns an object with .menuItems .menuNames members. This can be used to create a custom menu whose entries dynamically follow that of another menu for example. Simple m

- `exportOP` → OP (read-only)
  Raw: The

- `exportSource` → Channel (read-only)
  Raw: The object exporting to this parameter. Examples:

- `collapser` → bool (read-only)
  Raw: Returns True if the parameter is a parent of collapsable parameters (ie. a collapser).

- `collapsable` → bool (read-only)
  Raw: Returns True if the parameter is collapsable.

- `styleCloneImmune` → bool
  Raw: Get or set the parameter's style clone immunity. When

- `lastScriptChange` → tuple (read-only)
  Raw: Return information about when this parameter was last modified by a script. Cleared when the parameter is updated via the UI.

- `isDefault` → bool (read-only)
  Raw: True when the parameter value, expression and mode are in their default settings.

- `isCustom` → bool (read-only)
  Raw: True for

- `isPulse` → bool (read-only)
  Raw: True for pulse parameters.

- `isMomentary` → bool (read-only)
  Raw: True for momentary parameters.

- `isMenu` → bool (read-only)
  Raw: True for menu parameters.

- `isNumber` → bool (read-only)
  Raw: True for numeric parameters.

- `isFloat` → bool (read-only)
  Raw: True for floating point numeric parameters.

- `isInt` → bool (read-only)
  Raw: True for integer numeric parameters.

- `isOP` → bool (read-only)
  Raw: True for OP parameters.

- `isPython` → bool (read-only)
  Raw: True for python parameters.

- `isSequence` → bool (read-only)
  Raw: True for sequence header parameters.

- `isString` → bool (read-only)
  Raw: True for string parameters.

- `isToggle` → bool (read-only)
  Raw: True for toggle parameters.

- `valid` → bool (read-only)
  Raw: True if the referenced parameter currently exists, False if it has been deleted.

- `index` → int (read-only)
  Raw: A unique identifier for the parameter. May change in the case of custom parameters.

- `style` → str (read-only)
  Raw: Describes the behavior and contents of the custom parameter. Example

- `hidden` → bool (read-only)
  Raw: Get the parameter's hidden status. When True the parameter is considered obsolete or irrelevant and should not be modified. They are not shown in the dialog but only maintained for backward compatibil

### Methods
- `eval()` → Any
  Raw: Evaluate a parameter. This value may be derived by the parameter's constant value, expression, export, or bind, dependent on its mode.

- `evalNorm()` → float
  Raw: Similar to eval() but the returns the normalized slider value.

- `evalExpression()` → Any
  Raw: Evaluate the expression portion of a parameter, if it contains one. This will ignore any exports, etc.

- `evalExport()` → Any
  Raw: Evaluate the export portion of a parameter, if it contains one. This will ignore any expressions, etc.

- `evalOPs()` → List[OP]
  Raw: Evaluate the parameter as series of operators. This is useful for a custom parameter that specifies a list of operator paths for example.

- `evalFile()` → tdu.FileInfo
  Raw: Evaluate the parameter as a file path. This returns a

- `pulse(value=1, frames=0, seconds=0)` → None
  Raw: Pulsing sets a parameter to the specific value, cooks the operator, then restores the parameter to its previous value.

- `reset()` → bool
  Raw: Resets the parameter to its default state.

- `copy(Par)` → None
  Raw: Copy the specified

- `destroy()` → None
  Raw: Destroy the parameter referenced by this Par. An exception will be raised if the parameter has already been destroyed. Only custom and sequential parameters can be destroyed. Destroying a sequential p

- `isSamePar(par : Par)` → bool
  Raw: True if the provided Par is the same parameter on the same operator. Because

## ParameterCOMP_Class
Operator: ParameterCOMP (COMP)

### Members
- `minWidth` → int (read-only)
  Raw: The minimum width the parameter dialog can be drawn at before scaling is required.

## ParCollection_Class
Operator: ParCollection ()

### Members
- `owner` → OP (read-only)
  Raw: The

## ParGroup_Class
Operator: ParGroup ()

### Members
- `bindExpr` → Tuple[str, ...] (read-only)
  Raw: True if the provided parGroup is the same

- `bindMaster` → tuple (read-only)
  Raw: The objects to which this parameter is bound to, possibly None.

- `bindRange` → bool
  Raw: Get or set parameter's range binding state. If True, min, max, clampMin, clampMax, normMin, normMax, normVal values will be based on master bind parameter. Can only be set on Custom Parameters.

- `bindReferences` → tuple (read-only)
  Raw: The (possibly empty) lists of objects which bind to this parameter.

- `clampMax` → Tuple[float, ...]
  Raw: Get or set the parameter's numerical clamping behaviors. If set to

- `clampMin` → Tuple[bool, ...]
  Raw: Get or set the parameter's numerical clamping behaviors. If set to

- `collapsable` → bool (read-only)
  Raw: Returns True if the parameter is collapsable.

- `collapser` → bool (read-only)
  Raw: Returns True if the parameter is a parent of collapsable parameters (ie. a collapser).

- `default` → tuple
  Raw: Get or set the parameter's default values. Can only be set on Custom Parameters.

- `defaultExpr` → Tuple[str, ...]
  Raw: Get or set the parameter's default expressions. Can only be set on Custom Parameters.

- `defaultBindExpr` → Tuple[str, ...]
  Raw: Get or set the parameter's default bind expressions. Can only be set on Custom Parameters.

- `defaultMode` → Tuple[ParMode, ...]
  Raw: Get or set the parameter's evaluation modes.

- `enable` → bool
  Raw: Get or set the parameter group's enable state. Can only be set on

- `enableExpr` → str
  Raw: Get or set an expression that controls the enable state for this parameter group. This member is shared by all parameters and the parameter group.

- `exportOP` → Tuple[OP | None, ...] (read-only)
  Raw: The operators exporting to this parameter.

- `exportSource` → Tuple[Cell | Channel | None, ...] (read-only)
  Raw: tuple of objects exporting to this parameter. Examples: Cell, Channel or None.

- `expr` → Tuple[str, ...]
  Raw: Get or set the non-evaluated expressions only. To get the parameter's current values, regardless of the

- `help` → str
  Raw: Get or set a custom parameter's help text. To see any parameter's help, rollover the parameter while holding the Alt key.

- `isDefault` → bool (read-only)
  Raw: True when the parameter value, expression and mode are in their default settings.

- `isCustom` → bool (read-only)
  Raw: True for Custom Parameters.

- `isFloat` → bool (read-only)
  Raw: True for floating point numeric parameters.

- `isInt` → bool (read-only)
  Raw: True for integer numeric parameters.

- `isMenu` → bool (read-only)
  Raw: True for menu parameters.

- `isMomentary` → bool (read-only)
  Raw: True for momentary parameters.

- `isNumber` → bool (read-only)
  Raw: True for numeric parameters.

- `isOP` → bool (read-only)
  Raw: True for OP parameters.

- `isPulse` → bool (read-only)
  Raw: True for pulse parameters.

- `isPython` → bool (read-only)
  Raw: True for python parameters.

- `isSequence` → bool (read-only)
  Raw: True for sequence header parameters.

- `isString` → bool (read-only)
  Raw: True for string parameters.

- `isToggle` → bool (read-only)
  Raw: True for toggle parameters.

- `label` → str
  Raw: Get or set the parameter's label.

- `max` → Tuple[float, ...]
  Raw: Get or set the parameter's numerical maximum values. The parameter's values will be clamped at that maximum if

- `menuIndex` → Tuple[int, ...]
  Raw: Get or set a tuple of menu constant values by their indices.

- `menuLabels` → Tuple[list[str], ...]
  Raw: Get or set a tuple of lists of all possible menu choice labels. In the case of non menu parameters, None(s) are returned. Can only be set on Custom Parameters.

- `menuNames` → Tuple[list[str], ...]
  Raw: Get or set a tuple of lists of all possible menu choice names. In the case of non menu parameters, None(s) are returned. Can only be set on Custom Parameters.

- `menuSource` → Tuple[str, ...]
  Raw: Get or set a tuple of expressions that returns objects with

- `min` → Tuple[float, ...]
  Raw: Get or set the parameter's numerical minimum values. The parameter's values will be clamped at that minimum if

- `mode` → Tuple[ParMode, ...]
  Raw: Get or set the parameter's evaluation modes.

- `name` → str
  Raw: Get or set the parameter's unique name.

- `baseName` → str
  Raw: Get or set the parameter's base name. This excludes any sequence prefixes, sequence indices or name suffixes. Can only be set on Custom Parameters.

- `normMax` → Tuple[float, ...]
  Raw: Get or set the parameter's maximum slider values if the parameter is a numerical slider. Can only be set on Custom Parameters.

- `normMin` → Tuple[float, ...]
  Raw: Get or set the parameter's minimum slider values if the parameter is a numerical slider. Can only be set on Custom Parameters.

- `normVal` → Tuple[float, ...]
  Raw: Get or set the parameter's values as a normalized slider position. Can only be set on Custom Parameters.

- `order` → int
  Raw: Get or set the parameter's position on the parameter page. Can only be set on Custom Parameters.

- `owner` → OP (read-only)
  Raw: The OP to which this object belongs.

- `page` → Page
  Raw: Get or set the parameter page the custom parameter is part of. Can only be set on Custom Parameters.

- `password` → bool
  Raw: Get or set the parameter's password mode. When True all text is rendered as asterisks. Can only be set on Custom string, int or float parameters. Custom Parameters.

- `prevMode` → tuple (read-only)
  Raw: The parameter's previous evaluation modes.

- `readOnly` → bool
  Raw: Get or set the parameter's read only status. When True the parameter cannot be modified through the UI, only scripting.

- `startSection` → bool
  Raw: Get or set the parameter's separator status. When True a visible separator is drawn between this parameter and the ones preceding it. Can only be set on Custom Parameters.

- `style` → str (read-only)
  Raw: Describes the behavior and contents of the custom parameter. Example 'Float', 'Int', '

- `subLabel` → tuple (read-only)
  Raw: Returns the names of the sub-label.

- `val` → tuple
  Raw: Get or set the constant values of the parameter only. To get the parameter's current values, regardless of the Parameter Modes (

- `valid` → bool (read-only)
  Raw: True if the referenced parameter currently exists, False if it has been deleted.

- `index` → int (read-only)
  Raw: The parameter's order in the list.

- `sequence` → Sequence | None (read-only)
  Raw: The

- `sequenceBlock` → SequenceBlock | None (read-only)
  Raw: The

- `blockIndex` → int (read-only)
  Raw: The index of the parGroup within its

- `sequenceIndex` → int (read-only)
  Raw: The index of the parGroup's

- `size` → int
  Raw: Get or set the parameter's vector size.

- `maxSize` → int (read-only)
  Raw: Returns the maximum number of

- `defaultSize` → int (read-only)
  Raw: Returns the default number of

- `suffixes` → list (read-only)
  Raw: Returns a list of suffixes used to name the

### Methods
- `destroy()` → None
  Raw: Destroy the parameter referenced by this

- `eval()` → tuple
  Raw: Evaluate a parameter group. This value may be derived by the parameter group's constant value, expression, or export, dependent on its mode.

- `evalExport()` → tuple
  Raw: Evaluate the export portions of a parameter, if it contains any. This will ignore any expressions, etc.

- `evalExpression()` → tuple
  Raw: Evaluate the expression portions of a parameter, if it contains any. This will ignore any exports, etc.

- `evalNorm()` → Tuple[float, ...]
  Raw: Similar to eval() but the returns the normalized slider values.

- `evalOPs()` → Tuple[list[op], ...]
  Raw: Evaluate each parameter as a series of operators. This is useful for a custom parameter that specifies a list of operator paths for example.

- `pars(pattern)` → list
  Raw: Returns a (possibly empty) list of parameter objects that match the pattern.

- `reset()` → bool
  Raw: Resets the parameter group to its default state.

## ParGroupCollection_Class
Operator: ParGroupCollection ()

### Members
- `owner` → OP (read-only)
  Raw: The

## ParGroupPulse_Class
Operator: ParGroupPulse ()

### Methods
- `pulse(value, frames=0, seconds=0)` → None
  Raw: Pulsing sets a parameter to the specific value, cooks the operator, then restores the parameter to its previous value.

## ParGroupUnit_Class
Operator: ParGroupUnit ()

### Members
- `unit` → Par (read-only)
  Raw: The unit parameter in this ParGroupUnit object.

- `unitPar` → Par (read-only)
  Raw: The unit parameter in this ParGroupUnit object.

## ParticleSOP_Class
Operator: ParticleSOP (SOP)

### Methods
- `createParticles(num)` → List[Point]
  Raw: Creates a number of particles without advancing the particle simulation. Returns a list of points, where each point describes a particle.

## PatternCHOP_Class
Operator: PatternCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

- `sampleIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## PbrMAT_Class
Operator: PbrMAT (MAT)

### Methods
- `outputShader(includeGeoShader=False, instanceTexturing=False)` → glslMAT
  Raw: Will cause a new GLSL

## Peer_Class
Operator: Peer ()

### Members
- `owner` → OP (read-only)
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

- `port` → int (read-only)
  Raw: The network port associated with the peer.

- `address` → str (read-only)
  Raw: The network address associated with the peer.

- `hostname` → str (read-only)
  Raw: The network hostname associated with the peer.

### Methods
- `close()` → bool
  Raw: Close the peer connection. Returns True if successful. Closing a peer can be useful when implementing HTML server protocols for example.

- `sendBytes(*messages)` → int
  Raw: Send a sequence of bytes through this connection. No terminators are appended. Note: the message will not be OSC formatted.

- `sendOSC(address, *values, asBundle=False, useNonStandardTypes=True, use64BitPrecision=False)` → int
  Raw: Send an OSC formatted message through this connection. Multiple pairs of address/valueLists arguments to be passed. This method will automatically decide what OSC type to convert the passed values to.

- `send(*messages, terminator='')` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

## PhongMAT_Class
Operator: PhongMAT (MAT)

### Methods
- `outputShader(includeGeoShader=False, instanceTexturing=False)` → glslMAT
  Raw: Will cause a new GLSL

## PhotoshopinTOP_Class
Operator: PhotoshopinTOP (TOP)

### Members
- `isConnected` → bool (read-only)
  Raw: Is true if the operator is receiving image update. It will get updates when it's not locked to a particular document, or if it is locked and the document is opened in that Photoshop instance.

- `isReceivingUpdates` → bool (read-only)
  Raw: Is true if the operator is receiving image update. It will get updates when it's not locked to a particular document, or if it is locked and the document is opened in that Photoshop instance.

## Point_Class
Operator: Point ()

### Members
- `index` → int (read-only)
  Raw: Destroy and remove the actual point this object refers to. This operation is only valid when the primitive belongs to a

- `owner` → OP (read-only)
  Raw: The

- `P` → td.AttributeData
  Raw: The coordinates as

- `x` → float
  Raw: Get or set x coordinate value. This is the same as P[0].

- `y` → float
  Raw: Get or set y coordinate value. This is the same as P[1].

- `z` → float
  Raw: Get or set z coordinate value. This is the same as P[2].

- `normP` → tdu.Position (read-only)
  Raw: The normalized position of this point within the bounding box of the

### Methods
- `destroy()` → None
  Raw: Destroy and remove the actual point this object refers to. This operation is only valid when the primitive belongs to a

## Points_Class
Operator: Points ()

### Members
- `owner` → OP (read-only)
  Raw: Iterate over each point.

- `[index]` → td.Point
  Raw: Get a specific point given an integer index.

- `Iterator` → td.Point
  Raw: Iterate over each point.

### Methods
- `len(Points)` → int
  Raw: Returns the total number of points.

## PointSOP_Class
Operator: PointSOP (SOP)

### Members
- `inputColor` → Tuple[float, float, float, float] (read-only)
  Raw: The current point texture being evaluated, from the second input, or a default if not present, expressed as a 3-tuple (u, v, w).

- `inputColor2` → Tuple[float, float, float, float] (read-only)
  Raw: The current point color being evaluated, from the second input, or a default if not present, expressed as a 4-tuple (r, g, b, a).

- `inputNormal` → Tuple[float, float, float] (read-only)
  Raw: The current point normal being evaluated, from the first input, or a default if not present, expressed as a 3-tuple (n1, n2, n3).

- `inputNormal2` → Tuple[float, float, float] (read-only)
  Raw: The current point normal being evaluated, from the second input, or a default if not present, expressed as a 3-tuple (n1, n2, n3).

- `inputPoint` → Point (read-only)
  Raw: The current

- `inputPoint2` → Point (read-only)
  Raw: The current

- `inputTexture` → Tuple[float, float, float] (read-only)
  Raw: The current point texture being evaluated, from the first input, or a default if not present, expressed as a 3-tuple (u, v, w).

- `inputTexture2` → Tuple[float, float, float] (read-only)
  Raw: The current point texture being evaluated, from the second input, or a default if not present, expressed as a 3-tuple (u, v, w).

## Poly_Class
Operator: Poly ()

### Members
- `closed` → bool (read-only)
  Raw: Iterate over each vertex.

- `center` → TDU.Position
  Raw: Get or set the barycentric coordinate of this primitive. It is expressed as a TDU.Position object.

- `index` → int (read-only)
  Raw: The primitive index in the list.

- `normal` → TDU.Vector (read-only)
  Raw: The calculated normal vector of this primitive, expressed as a TDU.Vector object.

- `owner` → OP (read-only)
  Raw: The

- `weight` → float (read-only)
  Raw: The associated weight of the primitive. Only certain primitives, such as those created by the

- `direction` → TDU.Vector (read-only)
  Raw: A normalized vector pointing from the centroid of the

- `min` → TDU.Position (read-only)
  Raw: The minimum coordinates of this primitive along each dimension, expressed as a TDU.Position object.

- `max` → TDU.Position (read-only)
  Raw: The maximum coordinates of this primitive along each dimension, expressed as a TDU.Position object.

- `size` → TDU.Position (read-only)
  Raw: The size of this primitive along each dimension, expressed as a TDU.Position object.

- `[index]` → td.Vertex
  Raw: Get specific vertex given an integer index

- `Iterator` → td.Vertex
  Raw: Iterate over each vertex.

### Methods
- `destroy(destroyPoints=True)` → None
  Raw: Destroy and remove the actual primitive this object refers to. This operation is only valid when the primitive belongs to a

- `eval(u, v)` → TDU.Position
  Raw: Evaluate the

- `len(Prim)` → int
  Raw: Returns the total number of vertices.

## Position_Class
Operator: Position ()

### Members
- `x` → float
  Raw: Returns a new position with all 3 component's being negated.

- `y` → float
  Raw: Gets or sets the Y component of the position.

- `z` → float
  Raw: Gets or sets the Z component of the position.

### Methods
- `translate(x, y, z)` → None
  Raw: Translates the position by the specified values.

- `scale(x, y, z)` → None
  Raw: Scales each component of the position by the specified values.

- `copy()` → TDU.Position
  Raw: Returns a new position that is a copy of the position.

## Preferences_Class
Operator: Preferences ()

### Members
- `defaults` → dict (read-only)
  Raw: Iterate over each preference name.

- `[&lt;preference name&gt;]` → value
  Raw: Get or set specific preference given a preference name key.

- `Iterator` → str
  Raw: Iterate over each preference name.

### Methods
- `save()` → None
  Raw: Save preference values to disk. Unless saved, changes to preferences will be lost, next time application is started.

- `resetToDefaults()` → None
  Raw: Reset all preferences to their default values.

- `load()` → None
  Raw: Restore preference values from disk.

- `len(Preferences)` → int
  Raw: Returns the total number of preferences.

## Prim_Class
Operator: Prim ()

### Members
- `center` → TDU.Position (read-only)
  Raw: Iterate over each vertex.

- `index` → int (read-only)
  Raw: The primitive index in the list.

- `normal` → TDU.Vector (read-only)
  Raw: The calculated normal vector of this primitive, expressed as a TDU.Vector object.

- `owner` → OP (read-only)
  Raw: The

- `weight` → float (read-only)
  Raw: The associated weight of the primitive. Only certain primitives, such as those created by the

- `direction` → TDU.Vector (read-only)
  Raw: A normalized vector pointing from the centroid of the

- `min` → TDU.Position (read-only)
  Raw: The minimum coordinates of this primitive along each dimension, expressed as a TDU.Position object.

- `max` → TDU.Position (read-only)
  Raw: The maximum coordinates of this primitive along each dimension, expressed as a TDU.Position object.

- `size` → TDU.Position (read-only)
  Raw: The size of this primitive along each dimension, expressed as a TDU.Position object.

- `[index]` → td.Vertex
  Raw: Get specific vertex given an integer index

- `Iterator` → td.Vertex
  Raw: Iterate over each vertex.

### Methods
- `destroy(destroyPoints=True)` → None
  Raw: Destroy and remove the actual primitive this object refers to. This operation is only valid when the primitive belongs to a

- `eval(u, v)` → TDU.Position
  Raw: Evaluate the

- `len(Prim)` → int
  Raw: Returns the total number of vertices.

## PrimitiveSOP_Class
Operator: PrimitiveSOP (SOP)

### Members
- `inputColor` → Tuple[float, float, float, float] (read-only)
  Raw: The current

- `inputPrim` → Prim (read-only)
  Raw: The current

## Prims_Class
Operator: Prims ()

### Members
- `owner` → OP (read-only)
  Raw: Iterate over each prim.

- `[index]` → td.Prim
  Raw: Get a specific prim given an integer index.

- `Iterator` → td.Prim
  Raw: Iterate over each prim.

### Methods
- `len(Prims)` → int
  Raw: Returns the total number of prims.

## ProductEntry_Class
Operator: ProductEntry ()

### Members
- `licenseType` → int (read-only)
  Raw: The version of TouchDesigner this dongle product entry is valid for.

- `updateDate` → tuple[int, int, int] (read-only)
  Raw: The date the product entry is valid until. Returns a tuple in the form (YYYY, MM, DD).

- `version` → str (read-only)
  Raw: The version of TouchDesigner this dongle product entry is valid for.

## ProgressiveUnload_Class
Operator: ProgressiveUnload ()

### Members
- `active` → bool (read-only)
  Raw: Terminate the progressive unloading operation.

- `remainingNodes` → float (read-only)
  Raw: Returns the remaining nodes to be unloaded. Returns 0 if

- `nodesUnloaded` → float (read-only)
  Raw: Returns the number of unloaded nodes. Returns 0 if

- `lastFrameUnloadTime` → float (read-only)
  Raw: Returns the unload time (in milliseconds) of the latest frame. Returns 0 if

- `averageFrameUnloadTime` → float (read-only)
  Raw: Returns the average unload time (in milliseconds) across all previous frames. Returns nan if

- `gpuMemUnloaded` → float (read-only)
  Raw: Returns the total gpuMemory (in MB) that has been unloaded. Returns 0 if

- `elapsedFrames` → float (read-only)
  Raw: Returns the elapsed frames since progressive unloading started. Returns 0 if

### Methods
- `kill()` → None
  Raw: Terminate the progressive unloading operation.

## ProgressiveUnloader_Class
Operator: ProgressiveUnloader ()

### Members
- `active` → bool (read-only)
  Raw: Terminate the progressive unloading operation.

- `remainingNodes` → float (read-only)
  Raw: Returns the remaining nodes to be unloaded. Returns 0 if

- `nodesUnloaded` → float (read-only)
  Raw: Returns the number of unloaded nodes. Returns 0 if

- `lastFrameUnloadTime` → float (read-only)
  Raw: Returns the unload time (in milliseconds) of the latest frame. Returns 0 if

- `averageFrameUnloadTime` → float (read-only)
  Raw: Returns the average unload time (in milliseconds) across all previous frames. Returns nan if

- `gpuMemUnloaded` → float (read-only)
  Raw: Returns the total gpuMemory (in MB) that has been unloaded. Returns 0 if

- `elapsedFrames` → float (read-only)
  Raw: Returns the elapsed frames since progressive unloading started. Returns 0 if

### Methods
- `kill()` → None
  Raw: Terminate the progressive unloading operation.

## Project_Class
Operator: Project ()

### Members
- `folder` → str (read-only)
  Raw: Formatted contents of current python stack.

- `name` → str (read-only)
  Raw: The filename under which the project is saved.

- `saveVersion` → str (read-only)
  Raw: The

- `saveBuild` → str (read-only)
  Raw: The

- `saveTime` → str (read-only)
  Raw: The time and date the project was last saved.

- `saveOSName` → str (read-only)
  Raw: The

- `saveOSVersion` → str (read-only)
  Raw: The

- `paths` → dict (read-only)
  Raw: A dictionary which can be used to define URL-syntax path prefixes, enabling you to move your media to different locations easily. This dictionary is saved and loaded in the

- `cookRate` → float
  Raw: Get or set the maximum number of frames processed each second. In general you should not need to use this. It is preferred to look at the

- `realTime` → bool
  Raw: Get or set the real time cooking state. When True, frames may be skipped in order to maintain the cookRate. When False, all frames are processed sequentially regardless of duration. This is useful to 

- `isPrivate` → bool (read-only)
  Raw: True when the project networks cannot be directly viewed.

- `isPrivateKey` → bool (read-only)
  Raw: True when the private networks are accessible by a pass phrase.

- `cacheParameters` → bool
  Raw: Cache parameter values instead of always evaluating.

- `externalToxModifiedInProject` → bool (read-only)
  Raw: Get or set the window on top state.

- `windowStartMode` → WindowStartMode
  Raw: Get or set the window start mode. The mode is one of:

- `windowDraw` → bool
  Raw: Get or set the window drawing state.

- `windowStartCustomWidth` → int
  Raw: Get or set the window start width. Only used when windowStartMode is

- `windowStartCustomHeight` → int
  Raw: Get or set the window start height. Only used when windowStartMode is

- `windowStartCustomX` → int
  Raw: Get or set the window start X position. Only used when windowStartMode is

- `windowStartCustomY` → int
  Raw: Get or set the window start Y position. Only used when windowStartMode is

- `performOnStart` → bool
  Raw: Get or set the perform on start state.

- `performWindowPath` → OP
  Raw: Get or set the perform window path.

- `resetAudioOnDeviceChange` → bool
  Raw: Get or set whether audio devices momentarily reset when devices are added or removed to the system.

- `sdrReferenceWhiteNits` → int
  Raw: Get or set

- `hdrReferenceWhiteNits` → int
  Raw: Get or set

- `editorWindowPixelFormat` → WindowPixelFormat
  Raw: Get or set the pixel format the editor window will use. This is not the format

- `workingColorSpace` → WorkingColorSpace
  Raw: Get or set the working color space used for this project.

### Methods
- `load(path)` → None
  Raw: Load a specific

- `save(path, saveExternalToxs=False)` → bool
  Raw: Save the current session to disk. Returns True if a file was saved, False otherwise (eg, if the file exists, and when prompted, the user selects to not overwrite).

- `quit(force=False, crash=False)` → None
  Raw: Quit the project.

- `addPrivacy(key)` → bool
  Raw: Add privacy to a toe file with the given key.

- `removePrivacy(key)` → bool
  Raw: Completely remove privacy from a toe file.

- `accessPrivateContents(key)` → bool
  Raw: Gain access to a private file. The file will still be private the next time it is saved or re-opened.

- `applyWindowSettings()` → None
  Raw: Applies the project's window start settings to the current TouchDesigner window.

- `stack()` → str
  Raw: Formatted contents of current cook and parameter evaluation stack.

- `pythonStack()` → str
  Raw: Formatted contents of current python stack.

## Quaternion_Class
Operator: Quaternion ()

### Members
- `x` → float
  Raw: Applies the rotation of one quaternion to another quaternion.

- `y` → float
  Raw: Get or set the y component of the quaternion.

- `z` → float
  Raw: Get or set the z component of the quaternion.

- `w` → float
  Raw: Get or set the w component of the quaternion.

- `Quaternion *= Quaternion` → TDU.Quaternion
  Raw: Applies the rotation of one quaternion to another quaternion.

### Methods
- `lerp(q2, factor)` → TDU.Quaternion
  Raw: Returns the linear interpolation of the quaternion with another quaternion and an interpolation factor.

- `length()` → float
  Raw: Returns the length of the quaternion.

- `cross(q2)` → TDU.Vector
  Raw: Returns the cross product of the quaternion and argument.

- `rotate(vec)` → TDU.Vector
  Raw: Rotates a vector using the current quaternion. Returns a new vector.

- `slerp(q2, factor)` → TDU.Quaternion
  Raw: Returns the spherical interpolation of the quaternion with another quaternion and an interpolation factor.

- `eulerAngles(order='xyz')` → tuple
  Raw: Returns euler angles in degrees as a tuple (i.e. pitch as x, yaw as y, roll as z) from current quaternion and a rotation order. The 'order' argument can be set to any valid rotation order which by def

- `fromEuler(order='xyz')` → tuple
  Raw: Returns and set the current quaternion from euler angles in degrees as a 3 inputs argument (i.e. pitch as x, yaw as y, roll as z). The 'order' argument can be set to any valid rotation order which by 

- `axis()` → TDU.Vector
  Raw: Returns the rotation axis vector of the quaternion.

- `dot(q2)` → float
  Raw: Returns the dot product of the quaternion and the argument.

- `exp()` → TDU.Quaternion
  Raw: Returns the exponential of the quaternion as a new quaternion.

- `copy()` → TDU.Quaternion
  Raw: Creates a copy of the quaternion with separate values.

- `log()` → TDU.Quaternion
  Raw: Returns the natural logarithm of the current quaternion as a new quaternion.

- `inverse()` → None
  Raw: Invert the quaternion in place.

- `angle()` → float
  Raw: Returns the rotation angle (in degrees) of the quaternion.

## RenderpickCHOP_Class
Operator: RenderpickCHOP (CHOP)

### Members
- `pickedSOP` → OP (read-only)
  Raw: The

## RenderpickDAT_Class
Operator: RenderpickDAT (DAT)

### Methods
- `pick(u, v, camera=None)` → RenderPickEvent
  Raw: Perform a pick at the given (u, v)

## Run_Class
Operator: Run ()

### Members
- `active` → bool (read-only)
  Raw: Kill this run before it executes, and remove it from the global runs list, located in the

- `group` → str
  Raw: Get or set the group label associated with this script.

- `isCell` → bool (read-only)
  Raw: Returns true when the source is a

- `isDAT` → bool (read-only)
  Raw: Returns true when the source is a

- `isString` → bool (read-only)
  Raw: Returns true when the source is a string, from a td module run() call

- `path` → OP (read-only)
  Raw: The

- `remainingFrames` → int
  Raw: Get or set the remaining number of frames before the execution will occur.

- `remainingMilliseconds` → int
  Raw: Get or set the remaining number of milliseconds before the execution will occur.

- `source` → DAT | Cell | str (read-only)
  Raw: The source of the run. It will be either a

### Methods
- `kill()` → None
  Raw: Kill this run before it executes, and remove it from the global runs list, located in the

## ScalabledisplayTOP_Class
Operator: ScalabledisplayTOP (TOP)

### Members
- `cameraTransform` → tdu.Matrix (read-only)
  Raw: Gets the calibration approximate height as defined by calibration file.

- `projection` → tdu.Matrix (read-only)
  Raw: Gets the loaded projection

- `calibWidth` → int (read-only)
  Raw: Gets the calibration width as defined by calibration file.

- `calibHeight` → int (read-only)
  Raw: Gets the calibration height as defined by calibration file.

- `calibApproxWidth` → int (read-only)
  Raw: Gets the calibration approximate width as defined by calibration file.

- `calibApproxHeight` → int (read-only)
  Raw: Gets the calibration approximate height as defined by calibration file.

## ScriptCHOP_Class
Operator: ScriptCHOP (CHOP)

### Members
- `timeSliceDefault` → bool (read-only)
  Raw: Append a new channel to the

### Methods
- `copyNumpyArray(numpyArray, baseName='chan')` → None
  Raw: Copies the contents of the numpyArray into the

- `destroyCustomPars()` → int
  Raw: Remove all custom parameters from the

- `sortCustomPages(*pages)` → None
  Raw: Reorder custom parameter pages.

- `clear()` → None
  Raw: Remove all channels from the

- `appendCustomPage(name)` → Page
  Raw: Add a new

- `copy(chop)` → None
  Raw: Match all of this CHOPs channel data to the given

- `appendChan(name)` → Channel
  Raw: Append a new channel to the

## ScriptDAT_Class
Operator: ScriptDAT (DAT)

### Members
- `textFormat` → bool
  Raw: Reorder custom parameter pages.

### Methods
- `appendCustomPage(name)` → Page
  Raw: Add a new

- `destroyCustomPars()` → int
  Raw: Remove all custom parameters from the

- `sortCustomPages(*pages)` → None
  Raw: Reorder custom parameter pages.

## ScriptSOP_Class
Operator: ScriptSOP (SOP)

### Methods
- `clear()` → None
  Raw: Reorder custom parameter pages.

- `copy(sop)` → None
  Raw: Copy geometry from the specified

- `appendPoint()` → Point
  Raw: Append a

- `appendPoly(numVertices, closed=True, addPoints=True)` → Poly
  Raw: Append a

- `appendBezier(numVertices, closed=False, order=4, addPoints=True)` → Bezier
  Raw: Append a

- `appendMesh(numRows, numCols, closedU=False, closedV=False, addPoints=True)` → Mesh
  Raw: Append a

- `createPointGroup(str)` → None
  Raw: Creates a point

- `createPrimGroup(str)` → None
  Raw: Creates a primitive

- `appendCustomPage(name)` → Page
  Raw: Add a new

- `destroyCustomPars()` → int
  Raw: Remove all custom parameters from the

- `sortCustomPages(*pages)` → None
  Raw: Reorder custom parameter pages.

## ScriptTOP_Class
Operator: ScriptTOP (TOP)

### Methods
- `copyNumpyArray(numpyArray)` → None
  Raw: Reorder custom parameter pages.

- `copyCUDAMemory(address, size, shape, stream=None)` → None
  Raw: Copies the contents of the ptr, with given size and shape (

- `loadByteArray(extension, byteArray)` → bool
  Raw: Load the compressed file stored in byteArray into the

- `appendCustomPage(name)` → Page
  Raw: Add a new page of custom parameters. See

- `destroyCustomPars()` → int
  Raw: Remove all custom parameters from the

- `sortCustomPages(*pages)` → None
  Raw: Reorder custom parameter pages.

## Segment_Class
Operator: Segment ()

### Members
- `beginFrames` → int (read-only)
  Raw: The numeric index of this segment.

- `beginSamples` → int (read-only)
  Raw: The beginning point of the segment expressed in samples.

- `beginSeconds` → float (read-only)
  Raw: The beginning point of the segment expressed in seconds.

- `custom` → dict (read-only)
  Raw: dictionary of all the extra column values associated with the segment.

- `cycle` → bool (read-only)
  Raw: Whether or not the segment will repeat itself.

- `cycleEndAlertFrames` → int (read-only)
  Raw: The amount of time before cycling the callback will be executed, expressed in frames.

- `cycleEndAlertSamples` → int (read-only)
  Raw: The amount of time before cycling the callback will be executed, expressed in samples.

- `cycleEndAlertSeconds` → float (read-only)
  Raw: The amount of time before cycling the callback will be executed, expressed in seconds.

- `cycleLimit` → bool (read-only)
  Raw: Whether or not the segment will repeat itself indefinitely.

- `delayFrames` → int (read-only)
  Raw: The delay portion of the segment expressed in frames.

- `delaySamples` → int (read-only)
  Raw: The delay portion of the segment expressed in samples.

- `delaySeconds` → float (read-only)
  Raw: The delay portion of the segment expressed in seconds.

- `lengthFrames` → int (read-only)
  Raw: The length portion of the segment expressed in frames.

- `lengthSamples` → int (read-only)
  Raw: The length portion of the segment expressed in samples.

- `lengthSeconds` → float (read-only)
  Raw: The length portion of the segment expressed in seconds.

- `maxCycles` → int (read-only)
  Raw: The maximum number of repetitions.

- `owner` → OP (read-only)
  Raw: The OP to which this object belongs.

- `row` → int (read-only)
  Raw: Named tuple of all the parameter or column values describing the segment.

- `speed` → float (read-only)
  Raw: The speed multiplier of the segment.

- `index` → int (read-only)
  Raw: The numeric index of this segment.

## Sequence_Class
Operator: Sequence ()

### Members
- `blockSize` → int (read-only)
  Raw: Sorts the blocks based on the given

- `blocks` → list[SequenceBlock] (read-only)
  Raw: The set of all blocks in this sequence. A block is a set of parameters which can be repeated in an operator. See

- `maxBlocks` → int | None (read-only)
  Raw: The maximum number of blocks allowed in the sequence, or None if limitless.

- `name` → str
  Raw: Get or set the sequence name, which affects all its parameter names.

- `blockName` → str (read-only)
  Raw: Get the base name of the block name parameter. By default this is 'blockname' for builtin sequences and 'Blockname' for custom sequences.

- `numBlocks` → int
  Raw: Get or set the total number of parameter blocks in this sequence.

- `owner` → OP (read-only)
  Raw: The OP to which this object belongs.

- `blockPars` → list (read-only)
  Raw: An intermediate parameter collection object, from which specific sequence parameters can be found.

- `blockParGroups` → list (read-only)
  Raw: An intermediate parGroup collection object, from which specific sequence parameter groups can be found.

- `sequencePar` → Par (read-only)
  Raw: The main sequence parameter defining this sequence.

- `[block index]` → SequenceBlock
  Raw: Destroy the block of parameters at the given location.

### Methods
- `insertBlock(block : SequenceBlock)` → SequenceBlock
  Raw: Insert a block of parameters at the given location.

- `moveBlock(blockFrom, blockTo, num=1)` → None
  Raw: Move one or more blocks of parameters to the given location.

- `reorderBlocks(*indexes : int)` → None
  Raw: Reorder the specified blocks, leaving the rest in place.

- `sortBlocks(key=lambda Block, baseName="", reverse=False)` → None
  Raw: Sorts the blocks based on the given

## SequenceBlock_Class
Operator: SequenceBlock ()

### Members
- `index` → int (read-only)
  Raw: Destroy the block of parameters.

- `owner` → OP (read-only)
  Raw: The OP to which this object belongs.

- `par` → ParCollection (read-only)
  Raw: An intermediate parameter collection object, from which a specific sequence parameter can be found.

- `parGroup` → ParGroupCollection (read-only)
  Raw: An intermediate parameter collection object, from which a specific sequence parameter group can be found.

- `name` → str
  Raw: Get or set the sequence block name parameter value. This is implemented as a specifically named parameter in each block. The name is also used to identify a block by string via Sequence[<block name>].

- `namePar` → Par | None (read-only)
  Raw: The parameter defining the name for this block, or None if there isn't one.

- `sequence` → Sequence (read-only)
  Raw: The sequence object this block belongs to.

### Methods
- `destroy()` → None
  Raw: Destroy the block of parameters.

## SequenceCollection_Class
Operator: SequenceCollection ()

### Members
- `owner` → OP (read-only)
  Raw: The OP to which this object belongs.

## SerialDAT_Class
Operator: SerialDAT (DAT)

### Methods
- `sendBytes(*messages)` → int
  Raw: Send a sequence of strings through this connection.

- `send(*messages , terminator=' ')` → int
  Raw: Send a sequence of strings through this connection.

## SliderCOMP_Class
Operator: SliderCOMP (COMP)

### Methods
- `click(uOrV, v, clickCount=1, force=False, left=False, middle=False, right=False, vOnly=False)` → None
  Raw: Simulate a mouse click of the slider.

## SocketioDAT_Class
Operator: SocketioDAT (DAT)

### Methods
- `emit(event, data=None)` → None
  Raw: Emits an event to the socket.io server.

## St2110inTOP_Class
Operator: St2110inTOP (TOP)

### Members
- `inputSignalFormat` → str | None (read-only)
  Raw: True if any device is currently streaming to this operator..

- `isConnected` → bool (read-only)
  Raw: True if any device is currently streaming to this operator..

## SyncinCHOP_Class
Operator: SyncinCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Get a Timecode object for the timecode data representation of the last received index. See

## SyncoutCHOP_Class
Operator: SyncoutCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Send an auxiliary message. The message must be serializable. The message will come through the

### Methods
- `send(message)` → None
  Raw: Send an auxiliary message. The message must be serializable. The message will come through the

## SysInfo_Class
Operator: SysInfo ()

### Members
- `numCPUs` → int (read-only)
  Raw: The OS identifier of the selected

- `ram` → float (read-only)
  Raw: Amount of available RAM memory.

- `numMonitors` → int (read-only)
  Raw: The number of monitors.

- `xres` → int (read-only)
  Raw: The system's current monitor resolution width.

- `yres` → int (read-only)
  Raw: The system's current monitor resolution height.

- `tfs` → str (read-only)
  Raw: The path to the TFS directory.

- `MIDIInputs` → list[str] (read-only)
  Raw: A list of all MIDI Input device names.

- `MIDIOutputs` → list[str] (read-only)
  Raw: A list of all MIDI Output device names.

- `GPUName` → str (read-only)
  Raw: The name of the selected

- `GPUID` → str (read-only)
  Raw: The UUID of the selected

- `GPUPlatformID` → str (read-only)
  Raw: The OS identifier of the selected

## TableDAT_Class
Operator: TableDAT (DAT)

### Members
- `subRow` → int (read-only)
  Raw: Current header name for Table expressions.

- `subCol` → int (read-only)
  Raw: Current col index for Table expressions.

- `fillName` → str (read-only)
  Raw: Current header name for Table expressions.

## TcpipDAT_Class
Operator: TcpipDAT (DAT)

### Methods
- `sendBytes(*messages)` → int
  Raw: Send a sequence of strings through this connection.

- `send(*messages , terminator=' ')` → int
  Raw: Send a sequence of strings through this connection.

## TDU_Class
Operator: TDU ()

### Members
- `fileTypes` → dict (read-only)
  Raw: Returns a dictionary describing the builtin parameters of the OP class provided.

- `Matrix` → TDU.Matrix (read-only)
  Raw: The

- `Position` → TDU.Position (read-only)
  Raw: The

- `Vector` → TDU.Vector (read-only)
  Raw: The

- `Quaternion` → TDU.Quaternion (read-only)
  Raw: The

- `Color` → TDU.Color (read-only)
  Raw: The

- `FileInfo` → TDU.FileInfo (read-only)
  Raw: The FileInfo object takes a file path and has a few utility properties to provide additional information. It is derived from str, so will work as a Python string, but can be differentiated from a regu

- `ArcBall` → TDU.ArcBall (read-only)
  Raw: The

- `Camera` → tdu.Camera (read-only)
  Raw: The

- `debug` → module (read-only)
  Raw: Helper module for the builtin debug statement.

- `Timecode` → TDU.Timecode (read-only)
  Raw: The

### Methods
- `rand(seed)` → float
  Raw: Return a random value in the range [0.0, 1.0) given the input seed value. That is, it will never return 1.0, but it may return 0.0. For a given seed, it will always return the same random number. The 

- `clamp(inputVal, min, max)` → float | int
  Raw: Returns the input value clamped between min and max values. Arguments can be any type that can be compared (float, int, str, etc).

- `remap(inputVal, fromMin, fromMax, toMin, toMax)` → float
  Raw: Returns the input value remapped from the first range to the second.

- `base(str)` → str
  Raw: Returns the beginning portion of the string occurring before any digits. The search begins after the last slash if any are present.

- `digits(str)` → int | None
  Raw: Returns the numeric value of the last consecutive group of digits in the string, or None if not found. The search begins after the last slash if any are present. The digits do not nessearily need to b

- `validName(str)` → str
  Raw: Returns a version of the string suitable for an operator name. Converts illegal characters to underscores.

- `validPath(str)` → str
  Raw: Returns a version of the string suitable for an operator path, including slashes. Converts illegal characters to underscores.

- `expand(pattern)` → list
  Raw: Return a list of the expanded items, following the rules of

- `expandPath(path)` → str
  Raw: Expand the file path, using project.paths, the current folder, and any other relevant information.

- `collapsePath(path, asExpression=False)` → str
  Raw: Collapse the file path, using project.paths, the current folder, and any other relevant information.

- `split(string, eval=False)` → list
  Raw: Return a list from a space separated string, allowing quote delimiters.

- `match(pattern, inputList, caseSensitive=True)` → list
  Raw: Return a subset of inputList, in which each element matches the pattern. Wildcards are supported.

- `forceCrash()` → None
  Raw: forces a crash for debugging and crash recovery purposes

- `tryExcept(func1 : Callable, func2val : Any)` → Any
  Raw: Evaluate the first function (func1). If an exception is raised, return second argument instead. Second argument can be either a function, which will be called, or a final result.

- `ParMenu(menuNames, menuLabels=None)` → TDU.MenuObject
  Raw: This method uses a list of strings to create an object meant to be used as a

- `TableMenu(table, nameCol=0, labelCol=None, includeFirstRow=False)` → TDU.MenuObject
  Raw: Create a parameter menu source object based on a

- `isPointCloudFile(path)` → bool
  Raw: Check if the input file is a point cloud file. For .exr, the channel headers are checked. Returns true if channels ending in X,Y,Z or x,y,z are found, or if the file was saved out using Movie File Out

- `parSummary(OPType)` → dict
  Raw: Returns a dictionary describing the builtin parameters of the OP class provided.

## TextDAT_Class
Operator: TextDAT (DAT)

### Members
- `textFormat` → bool
  Raw: Get or set text format flag. This will convert between table and text formats. Note: Adding a row or column after will convert the

## TextLine_Class
Operator: TextLine ()

### Members
- `glyph` → int (read-only)
  Raw: The width of the format box of this line of text. This member is not used by the

- `fontIndex` → int (read-only)
  Raw: The index of the font that the glyph belongs to. Glyphs are not interchangable between fonts.

- `text` → str (read-only)
  Raw: The text for this line.

- `origin` → tdu.Position (read-only)
  Raw: A tdu.Position object that gives the baseline origin of the line of text.

- `lineWidth` → float (read-only)
  Raw: The width of the format box of this line of text. This member is not used by the

## TextSOP_Class
Operator: TextSOP (SOP)

### Members
- `numLines` → int (read-only)
  Raw: Get the number of lines of the outputted text, after operations such as word-wrap have been applied.

- `ascender` → float (read-only)
  Raw: The ascender of the font, as described by the font's metrics.

- `descender` → float (read-only)
  Raw: The descender of the font, as described by the font's metrics.

- `capHeight` → float (read-only)
  Raw: The cap height of the font, as described by the font's metrics. This is usually the height of a capital H.

- `xHeight` → float (read-only)
  Raw: The x height of the font, as described by the font's metrics. This is usually the height of a lower case x.

- `lineGap` → float (read-only)
  Raw: The suggested gap between lines, as described by the font's metrics.

- `numGlyphs` → int (read-only)
  Raw: The number of glyphs that were generated. Note that this isn't nessesarily the number of characters (code points) in the original string.

### Methods
- `fontSupportsChars(str)` → bool
  Raw: Returns True if every character maps to a glyph. This doesn't mean the font supports the language in all cases. Glyphs that come from ligatures etc. may still be missing from the font.

- `lines()` → List[TextLine]
  Raw: Get the number of lines of the outputted text, after operations such as word-wrap have been applied.

## TextTOP_Class
Operator: TextTOP (TOP)

### Members
- `curText` → str (read-only)
  Raw: Returns a list of

- `cursorEnd` → int
  Raw: Get or set cursor end position, when used with a

- `cursorStart` → int
  Raw: Get or set cursor start position, when used with a

- `selectedText` → str (read-only)
  Raw: Selected contents, when used with a

- `textHeight` → int (read-only)
  Raw: Calculated height of text, in pixels. This value does

- `textWidth` → int (read-only)
  Raw: Calculated width of text, in pixels. This value

- `numLines` → int (read-only)
  Raw: Get the number of lines of the outputted text, after operations such as word-wrap have been applied.

- `ascender` → float (read-only)
  Raw: The ascender of the font, as described by the font's metrics.

- `descender` → float (read-only)
  Raw: The descender of the font, as described by the font's metrics.

- `capHeight` → float (read-only)
  Raw: The cap height of the font, as described by the font's metrics. This is usually the height of a capital H.

- `xHeight` → float (read-only)
  Raw: The x height of the font, as described by the font's metrics. This is usually the height of a lower case x.

- `lineGap` → float (read-only)
  Raw: The suggested gap between lines, as described by the font's metrics.

### Methods
- `fontSupportsChars(str)` → bool
  Raw: Returns True if every character maps to a glyph. This doesn't mean the font supports the language in all cases. Glyphs that come from ligatures etc. may still be missing from the font.

- `evalTextSize(str)` → Tuple[float, float]
  Raw: Returns a tuple of width, height of the given string using the operators settings. Does not include the word wrap and auto size font options.

- `lines()` → List[TextLine]
  Raw: Returns a list of

## Timecode_Class
Operator: Timecode ()

### Members
- `countdown` → tdu.Timecode (read-only)
  Raw: Set Timecode to a custom length. Useful in conjunction with countdown.

- `dropFrame` → bool (read-only)
  Raw: True if the Timecode is drop-frame, False otherwise.

- `fps` → float
  Raw: Get or set the framerate (in frames per second) of the Timecode.

- `frame` → int (read-only)
  Raw: The Timecode frame: 0 to fps-1

- `hour` → int (read-only)
  Raw: The Timecode hour: 0 to 99 if non-SMPTE, 0 to 23 otherwise.

- `minute` → int (read-only)
  Raw: The Timecode minute: 0 to 59.

- `second` → int (read-only)
  Raw: The Timecode second: 0 to 59.

- `negative` → bool
  Raw: True if the Timecode is negative, and False otherwise. Always False if the Timecode is following SMPTE standard.

- `smpte` → bool
  Raw: True if the Timecode is SMPTE standard, and False otherwise. SMPTE Timecodes cannot be negative and cannot exceed 24 hours.

- `text` → str (read-only)
  Raw: Get the text format of the Timecode.

- `totalFrames` → int (read-only)
  Raw: The total number of Timecode frames, which is calculated from the hour, minute, second, frame values. Whether or not the Timecode is drop frame will also affect the value.

- `totalSeconds` → float (read-only)
  Raw: The total number of Timecode seconds, which is calculated from the hour, minute, second, frame values. Whether or not the Timecode is drop frame will also affect the value.

- `cycle` → bool
  Raw: Get or set whether the timecode cycles. True if the Timecode cycles when the timecode value reaches the custom length (ie. specified with setLength()). If False then the timecode value will hold the l

### Methods
- `setComponents(hour, minute, second, frame, negative=False, fps=None)` → None
  Raw: Set the Timecode from individual time components.

- `setString(timecodeStr, fps=None)` → None
  Raw: Set the Timecode from a string formated as [-]hh:mm:ss:ff.

- `setTotalFrames(totalFrames, fps=None)` → None
  Raw: Set the Timecode to a single integer value representing the new total frames.

- `setLength(length)` → None
  Raw: Set Timecode to a custom length. Useful in conjunction with countdown.

## TimeCOMP_Class
Operator: TimeCOMP (COMP)

### Members
- `frame` → float
  Raw: Get or set time signature, second value.

- `seconds` → float
  Raw: Get or set the current time output by this component (expressed in seconds).

- `rate` → float
  Raw: Get or set the frames per second, (or rate).

- `play` → bool
  Raw: Get or set whether the component is playing.

- `timecode` → str
  Raw: Get or set the current timecode generated by this component.

- `start` → float
  Raw: Get or set start of main frame range.

- `end` → float
  Raw: Get or set end of main frame range.

- `rangeStart` → float
  Raw: Get or set start of sub frame range. Must be within main start, end range

- `rangeEnd` → float
  Raw: Get or set end of sub frame range. Must be within main start, end range.

- `loop` → bool
  Raw: Get or set whether the timeline loops.

- `independent` → float
  Raw: Get or set whether the timeline runs independently of other timelines.

- `tempo` → float
  Raw: Get or set beats per minute.

- `signature1` → int
  Raw: Get or set time signature, first value.

- `signature2` → int
  Raw: Get or set time signature, second value.

## TimelineCHOP_Class
Operator: TimelineCHOP (CHOP)

### Members
- `timecode` → tdu.Timecode (read-only)
  Raw: Get a Timecode object for the timecode data representation of the current timeline frame. See

## TimerCHOP_Class
Operator: TimerCHOP (CHOP)

### Members
- `beginFrame` → list (read-only)
  Raw: Sets the current cycle to be the last cycle of the current segment. Equivalent to pulsing the Exit at End of Cycle parameter on the Timer Page.

- `beginSample` → list (read-only)
  Raw: Get a list of begin values in samples. 0-based.

- `beginSeconds` → list (read-only)
  Raw: Get a list of begin values in seconds.

- `cumulativeFrames` → int (read-only)
  Raw: Get the cumulative time expressed in frames. See

- `cumulativeSamples` → int (read-only)
  Raw: Get the cumulative time expressed in samples. See

- `cumulativeSeconds` → float (read-only)
  Raw: Get the cumulative time expressed in seconds. It counts from 0 when you Start. Unlike

- `cumulativeTimecode` → tdu.Timecode (read-only)
  Raw: Get the cumulative time as a Timecode

- `masterFrames` → int
  Raw: Get or set the master time expressed in frames. 0-based. See

- `masterSamples` → int
  Raw: Get or set the master time expressed in samples. See

- `masterSeconds` → float
  Raw: Get or set the master time expressed in seconds. It counts from 0 when you Start,

- `masterFraction` → float
  Raw: Get or set the master time expressed in fractional form. See

- `masterTimecode` → tdu.Timecode
  Raw: Get or set the master time expressed as a Timecode

- `cycle` → float
  Raw: Get or set the cycle index of the current segment.

- `fraction` → float (read-only)
  Raw: Get the time index in fractional form, same as the

- `playingFrames` → int (read-only)
  Raw: Get the playing time expressed in frames. 0-based. See

- `playingSamples` → int (read-only)
  Raw: Get the playing time expressed in samples. See

- `playingSeconds` → float (read-only)
  Raw: Get the playing time expressed in seconds. It counts from 0 when you Start. it is unaffected by the Speed parameter, but unlike

- `playingTimecode` → tdu.Timecode (read-only)
  Raw: Get the playing time as a Timecode

- `runningFraction` → float
  Raw: Get the running time index expressed in fractional form. See

- `runningFrames` → float
  Raw: Get the running time expressed in frames. 0-based. See

- `runningSamples` → float
  Raw: Get the running time index expressed in samples. See

- `runningSeconds` → float
  Raw: Get the running time expressed in seconds. It keeps counting up after Start and is not affected by changing the Speed or pausing Play or scrubbing. It is basically the "wall clock" after pressing Star

- `runningTimecode` → tdu.Timecode
  Raw: Get the running time index as a Timecode

- `runningLengthFrames` → float (read-only)
  Raw: Get the running length expressed in frames.

- `runningLengthSamples` → float (read-only)
  Raw: Get the running length expressed in samples.

- `runningLengthSeconds` → float (read-only)
  Raw: Get the running length expressed in seconds.

- `runningLengthTimecode` → tdu.Timecode (read-only)
  Raw: Get the running length as a Timecode

- `segment` → float
  Raw: Get or set the segment index.

- `segments` → list (read-only)
  Raw: Get the list of segments.

- `timecode` → tdu.Timecode (read-only)
  Raw: Get the master timecode. See

### Methods
- `goToNextSegment()` → None
  Raw: Jump to the next segment. Equivalent to pulsing the Go to Next Segment parameter on the Segments Page.

- `goToCycleEnd()` → None
  Raw: Jump to the end of the current cycle. Equivalent to pulsing the Go to End of Cycle parameter on the Timer Page.

- `goTo(segment=0, cycle=0, endOfCycle=True, seconds=0, frame=0, sample=0,  fraction=0)` → None
  Raw: Allows the user to jump to a different time index based on the arguments passed in. Only one unit of time (seconds, frame, sample, fraction) can be specified when calling this method. For example, inc

- `goToPrevSegment()` → None
  Raw: Jump to the previous segment. Equivalent to pulsing the Go to Previous Segment parameter on the Segments Page.

- `lastCycle()` → None
  Raw: Sets the current cycle to be the last cycle of the current segment. Equivalent to pulsing the Exit at End of Cycle parameter on the Timer Page.

## UdpinDAT_Class
Operator: UdpinDAT (DAT)

### Methods
- `sendBytes(*messages)` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

- `sendOSC(address, *values, asBundle=False, useNonStandardTypes=True, use64BitPrecision=False)` → int
  Raw: Send an OSC formatted message through this connection. Multiple pairs of address/valueLists arguments to be passed. This method will automatically decide what OSC type to convert the passed values to.

- `send(*messages, terminator='')` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

## UdtinDAT_Class
Operator: UdtinDAT (DAT)

### Methods
- `sendBytes(*messages)` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

- `sendOSC(address, *values, asBundle=False, useNonStandardTypes=True, use64BitPrecision=False)` → int
  Raw: Send an OSC formatted message through this connection. Multiple pairs of address/valueLists arguments to be passed. This method will automatically decide what OSC type to convert the passed values to.

- `send(*messages, terminator='')` → int
  Raw: Send a sequence of strings through this connection. Note: the message will not be OSC formatted.

## UI_Class
Operator: UI ()

### Members
- `clipboard` → str (read-only)
  Raw: Given an external filename, finds the corresponding

- `colors` → Colors (read-only)
  Raw: Access to the application

- `dpiBiCubicFilter` → bool
  Raw: Get or set the global DPI scale filtering mode of TouchDesigner windows. True means bi-cubic, False means linear.

- `masterVolume` → float
  Raw: Get or set the master audio output volume. A value of 0 is no output, while a value of 1 is full output.

- `options` → Options (read-only)
  Raw: Access to the application

- `panes` → Panes (read-only)
  Raw: Access to the set of all

- `performMode` → bool
  Raw: Get or set

- `preferences` → Preferences (read-only)
  Raw: Access to the application

- `redrawMainWindow` → bool
  Raw: Get or set whether the main window should redraw. The main window is either the main network editor, or the perform window.

- `rolloverOp` → OP | None (read-only)
  Raw: Operator currently under the mouse in a network editor.

- `rolloverPar` → Par | None (read-only)
  Raw: Parameter currently under the mouse in a parameter dialog.

- `rolloverParGroup` → ParGroup | None (read-only)
  Raw: returns the panel currently under the mouse.

- `rolloverPage` → Page | None (read-only)
  Raw: returns the Page currently under the mouse in a parameter dialog.

- `rollover` → Par | ParGroup | Page | OP | PanelCOMP | None (read-only)
  Raw: combines the other rollover members into one test, reporting the TD object under the mouse. Because there is some overlap, priority is in the following order: Par |

- `lastChopChannelSelected` → Channel (read-only)
  Raw: Last

- `showPaletteBrowser` → bool
  Raw: Get or set display of the palette browser.

- `status` → str
  Raw: Get or set the status message.

- `undo` → Undo (read-only)
  Raw: The

- `windowWidth` → int (read-only)
  Raw: Get the app window width.

- `windowHeight` → int (read-only)
  Raw: Get the app window height.

- `windowX` → int (read-only)
  Raw: Get the app window X position.

- `windowY` → int (read-only)
  Raw: Get the app window Y position.

### Methods
- `copyOPs(listOfOPs)` → None
  Raw: Copy a list of operators to the operator clipboard. All operators must be children of the same component.

- `messageBox(title, message, buttons=['Ok'])` → int
  Raw: This method will open a message dialog box with the specified message. Returns the index of the button clicked.

- `refresh()` → None
  Raw: Update and redraw all viewports, nodes, UI elements etc immediately. This update is otherwise done once per frame at the end of all script executions. For example, if the current frame is manually cha

- `chooseFile(load=True, start=None, fileTypes=None, title=None, asExpression=False)` → str | None
  Raw: Open a dialog box for loading or saving a file. Returns the filename selected or None if the dialog is cancelled.

- `viewFile(URL_or_path, showInFolder=False)` → None
  Raw: View a URL or file in the default external application. You can use

- `openBeat()` → None
  Raw: Open the

- `openBookmarks()` → None
  Raw: Open the

- `openCOMPEditor(path)` → None
  Raw: Open component editor for the specific operator.

- `openConsole()` → None
  Raw: Open the

- `openDialogHelp(title)` → None
  Raw: Open help page for the specific dialog.

- `openErrors()` → None
  Raw: Open the

- `openExplorer()` → None
  Raw: Open an Explorer window.

- `openExportMovie(path)` → None
  Raw: Open the

- `openImportFile()` → None
  Raw: Open the

- `openKeyManager()` → None
  Raw: Open the

- `openMIDIDeviceMapper()` → None
  Raw: Open the

- `openNewProject()` → None
  Raw: Open the

- `openOperatorSnippets(family=None, type=None, example=None)` → None
  Raw: Open the Operator

- `openPaletteBrowser()` → None
  Raw: Open the

- `openPerformanceMonitor()` → None
  Raw: Open the

- `openPreferences()` → None
  Raw: Open the

- `openSearch()` → None
  Raw: Open the

- `openTextport()` → None
  Raw: Open the

- `openVersion()` → None
  Raw: Open a dialog displaying current version information. See also:

- `openWindowPlacement()` → None
  Raw: Open the

- `findEditDAT(filename)` → DAT | None
  Raw: Given an external filename, finds the corresponding

## Undo_Class
Operator: Undo ()

### Members
- `globalState` → bool (read-only)
  Raw: Terminate an undo block.

- `redoStack` → list (read-only)
  Raw: A list of names for redo operations available.

- `state` → bool (read-only)
  Raw: Is undo enabled or not.

- `undoStack` → list (read-only)
  Raw: A list of names for undo operations available.

### Methods
- `startBlock(name, enable=True)` → None
  Raw: Start a named undo block. While the block is active, any script changes to TouchDesigner objects that are undoable in the editor will be added to the undo stack.

- `clear()` → None
  Raw: Clear undo and redo stack. This will terminate any current undo blocks.

- `addCallback(callback, info=None)` → None
  Raw: Add a Python callback into the current undo block.

- `redo()` → None
  Raw: Redo the next operation. This will terminate any current undo blocks.

- `undo()` → None
  Raw: Undo the last operation. This will terminate any current undo blocks.

- `endBlock()` → None
  Raw: Terminate an undo block.

## Vector_Class
Operator: Vector ()

### Members
- `x` → float
  Raw: Returns a new vector with all 3 components being negated.

- `y` → float
  Raw: Gets or sets the Y component of the vector.

- `z` → float
  Raw: Gets or sets the Z component of the vector.

### Methods
- `angle(vec)` → float
  Raw: Returns the angel (in degrees) between the current vector and specified vector (vec).

- `scale(x, y, z)` → None
  Raw: Scales each component of the vector by the specified values.

- `normalize()` → None
  Raw: Makes the length of this vector 1.

- `length()` → float
  Raw: Returns the length of this vector.

- `lengthSquared()` → float
  Raw: Returns the squared length of this vector.

- `copy()` → TDU.Vector
  Raw: Returns a new vector that is a copy of the vector.

- `distance(vec)` → float
  Raw: Returns the distance of the current vector to specified vector (vec).

- `lerp(vec2, t)` → TDU.Vector
  Raw: Returns the linear interpolation of this vector and vec2. That is vec1 * (1.0 - t) + vec2 * t, where vec1 is the current vector. The value for t is not restricted to the range [0, 1].

- `slerp(vec2, t)` → TDU.Vector
  Raw: Returns the spherical interpolation of this vector and vec2. The value for t is not restricted to the range [0, 1].

- `dot(vec)` → float
  Raw: Returns the dot product of this vector and the passed vector.

- `cross(vec)` → TDU.Vector
  Raw: Returns the cross product of this vector and the passed vector. The operation is self cross vec.

- `project(vec1, vec2)` → None
  Raw: Projects this vector onto the plan defined by vec1 and vec2. Both vec1 and vec2 must be normalized. The result may not be normalized.

- `reflect(vec)` → None
  Raw: Reflects the current vector about the specified vector (vec).

## Vertex_Class
Operator: Vertex ()

### Members
- `index` → int (read-only)
  Raw: The

- `owner` → OP (read-only)
  Raw: The

- `point` → td.Point
  Raw: Get or set the

- `prim` → td.Prim (read-only)
  Raw: The

## VFS_Class
Operator: VFS ()

### Members
- `owner` → OP (read-only)
  Raw: Iterate over each virtual file name.

- `[name]` → VFSFile
  Raw: Add an embedded file from a bytearray to the component. Returns a VFSFile instance of the added file. To delete the file, see

- `Iterator` → str
  Raw: Iterate over each virtual file name.

### Methods
- `addFile(filePath, overrideName=None)` → VFSFile
  Raw: Add an embedded file from disk to the component with an option to override the name. Returns a VFSFile instance of the added file. To delete the file, see

- `export(folder, pattern='*', overwrite=False)` → list
  Raw: Exports any matching files to the folder on disk. If overwrite is True then any existing files on disks with the same name will be overwritten. Returns a list of paths on disk to the exported files.

- `find(pattern='*')` → list
  Raw: Finds all files in

## VFSFile_Class
Operator: VFSFile ()

### Members
- `name` → str (read-only)
  Raw: Exports the file to the specified folder on disk and returns the location.

- `size` → int (read-only)
  Raw: Get the size of the file data.

- `date` → "datetime.datetime" (read-only)
  Raw: Get the modified date of the file in the form of a datetime Python object.

- `virtualPath` → str (read-only)
  Raw: Get the virtual path of the file. Returns a String formatted for fetching the file data from

- `originalFilePath` → str (read-only)
  Raw: Get the original file path on disk. If the VFSFile was created from a bytearray and not a file on disk then this will be empty.

- `owner` → OP (read-only)
  Raw: Get the OP owner.

- `byteArray` → bytearray
  Raw: Get or set the file data as a bytearray.

### Methods
- `destroy()` → None
  Raw: Destroys the file in

- `export(folder)` → str
  Raw: Exports the file to the specified folder on disk and returns the location.

## VideodeviceinTOP_Class
Operator: VideodeviceinTOP (TOP)

### Members
- `isConnected` → bool (read-only)
  Raw: If the device supports timecode, then returns a Timecode object for the latest received frame. see

- `inputSignalFormat` → str (read-only)
  Raw: If available for the current Library, returns a string for the input signal format. This string can be used to set the 'Signal Format' menu on the Video Device Out

- `timecode` → tdu.Timecode (read-only)
  Raw: If the device supports timecode, then returns a Timecode object for the latest received frame. see

## VideostreaminTOP_Class
Operator: VideostreaminTOP (TOP)

### Members
- `connectionsFailed` → int (read-only)
  Raw: Unloads the video stream and frees it's memory usage. The stream will open again next time it cooks, so make sure nothing is still using it to keep it closed.

- `connectionsLost` → int (read-only)
  Raw: The number of times this operator has lost a connection it has previous successfully established.

- `frameTime` → float (read-only)
  Raw: The timestamp of the currently shown frame, in seconds.

- `isConnected` → bool (read-only)
  Raw: True if connected to target URL.

- `isConnecting` → bool (read-only)
  Raw: True if attempting to connect to target URL.

- `isOddField` → bool (read-only)
  Raw: When de-interlacing, this tells if the odd field is currently being shown.

- `videoHeight` → int (read-only)
  Raw: Height of the movie, in pixels.

- `videoWidth` → int (read-only)
  Raw: Width of the movie, in pixels.

### Methods
- `unload(cacheMemory=False)` → None
  Raw: Unloads the video stream and frees it's memory usage. The stream will open again next time it cooks, so make sure nothing is still using it to keep it closed.

## VideostreamoutTOP_Class
Operator: VideostreamoutTOP (TOP)

### Members
- `streamURL` → str (read-only)
  Raw: The URL to connect to this operator's stream.

## WaveCHOP_Class
Operator: WaveCHOP (CHOP)

### Members
- `chanIndex` → int (read-only)
  Raw: The index of the current sample being evaluated.

## WebclientDAT_Class
Operator: WebclientDAT (DAT)

### Members
- `connections` → list (read-only)
  Raw: Closes the active connection.

### Methods
- `request(url, method, header=None, data=None, pars=None, authType=None, username=None, password=None, appKey=None, appSecret=None, oauth1Token=None, oauth1Secret=None, oauth2Token=None, uploadFile=None, timeout=60000)` → int
  Raw: Sends an HTTP request of the specified method to the url. Any headers and data can also be specified. Returns the connection identifier that will correspond with the id passed to onResponse callbacks.

- `closeConnection(id)` → None
  Raw: Closes the active connection.

## WebrenderTOP_Class
Operator: WebrenderTOP (TOP)

### Members
- `loaded` → bool (read-only)
  Raw: Flush current cookies the local storage.

### Methods
- `interactMouse(u, v, leftClick=0, middleClick=0, rightClick=0, left=False, middle=False, right=False, wheel=0, pixels=False)` → None
  Raw: Send mouse clicks, rollovers, moves and drags to the webpage.

- `interactTouch(u, v, pixels=False, radiusx=1, radiusy=1, start=-1, move=-1, end=-1)` → None
  Raw: Send touch events to the webpage.

- `executeJavaScript(script)` → None
  Raw: Execute a line of javascript on the current webpage.

- `sendKey(key, char=None, shift=False, alt=False, ctrl=False, cmd=False)` → None
  Raw: Send key characters to the webpage.

- `sendString(char)` → None
  Raw: Send a string of characters to the webpage.

- `flushCookieStore()` → None
  Raw: Flush current cookies the local storage.

## WebrtcDAT_Class
Operator: WebrtcDAT (DAT)

### Methods
- `addIceCandidate(connectionId, candidate, lineIndex, sdpMid)` → None
  Raw: Set the description (ie. SDP) of the remote side of the peer-to-peer connection.

- `addTrack(connectionId, trackId, type)` → bool
  Raw: Adds a local media track to the connection and return true if it was successfully added.

- `closeConnection(connectionId)` → None
  Raw: Closes the connection.

- `closeDataChannel(connectionId, channelName)` → bool
  Raw: Closes the data channel. Returns true if successful.

- `createAnswer(connectionId)` → None
  Raw: Creates a local answer for the connection associated with the ID.

- `createDataChannel(connectionId, channelName)` → bool
  Raw: Creates a bidirectional data channel over the connection. Returns true if successful.

- `createOffer(connectionId)` → None
  Raw: Creates a local offer for the connection associated with the ID.

- `getConnectionState(connectionId)` → str
  Raw: * connectionId - (Keyword, Optional) The UUID of the connection.

- `getSignalingState(connectionId)` → str
  Raw: * connectionId - (Keyword, Optional) The UUID of the connection.

- `getIceGatheringState(connectionId)` → str
  Raw: * connectionId - (Keyword, Optional) The UUID of the connection.

- `getIceConnectionState(connectionId)` → str
  Raw: * connectionId - (Keyword, Optional) The UUID of the connection.

- `getStats(connectionId=None)` → str
  Raw: Fetches the JSON-formated statistics of any or all connections.

- `getTracks(connectionId, type, peer)` → list
  Raw: Returns a list of track IDs.

- `openConnection()` → str
  Raw: Opens the local end of a peer connection and returns the UUID.

- `removeTrack(connectionId, trackId, type)` → bool
  Raw: Removes a local media track from the connection and return true if it was successfully removed.

- `restartIce(connectionId)` → None
  Raw: Restarts the ICE gathering process.

- `sendBytes(connectionId, channelName, data)` → None
  Raw: Send bytes over the data channel.

- `sendText(connectionId, channelName, data)` → None
  Raw: Send text over the data channel.

- `setLocalDescription(connectionId, type, sdp, stereo=False)` → None
  Raw: Set the description (ie. SDP) of the local side of the peer-to-peer connection.

- `setRemoteDescription(connectionId, type, sdp)` → None
  Raw: Set the description (ie. SDP) of the remote side of the peer-to-peer connection.

## WebserverDAT_Class
Operator: WebserverDAT (DAT)

### Methods
- `authenticateBasic(token, userPasswords)` → bool
  Raw: Sends pong data to the client's Web Socket.

- `webSocketClose(client)` → None
  Raw: Closes the Web Socket connection.

- `webSocketSendBinary(client, data)` → None
  Raw: Sends binary data to the client's Web Socket.

- `webSocketSendText(client, data)` → None
  Raw: Sends text data to the client's Web Socket.

- `webSocketSendPing(client, data=None)` → None
  Raw: Sends ping data to the client's Web Socket.

- `webSocketSendPong(client, data=None)` → None
  Raw: Sends pong data to the client's Web Socket.

## WebsocketDAT_Class
Operator: WebsocketDAT (DAT)

### Methods
- `sendText(message)` → int
  Raw: Send a binary frame over the WebSocket connection. Returns the number of bytes sent in the message, or a negative value on error.

- `sendPong(contents)` → int
  Raw: Send a pong reply over the WebSocket connection. Returns the number of bytes sent in the message, or a negative value on error.

- `sendPing(contents)` → int
  Raw: Send a ping request over the WebSocket connection. Returns the number of bytes sent in the message, or a negative value on error.

- `sendBinary(contents)` → int
  Raw: Send a binary frame over the WebSocket connection. Returns the number of bytes sent in the message, or a negative value on error.

## WindowCOMP_Class
Operator: WindowCOMP (COMP)

### Members
- `scalingMonitorIndex` → int (read-only)
  Raw: Activates the window, sets it to the foregound and other visual cues. Sets focus and increases process priority.

- `isBorders` → bool (read-only)
  Raw: True if the window is bordered.

- `isFill` → bool (read-only)
  Raw: True if the window will stretch its contents to fill its specified area.

- `isOpen` → bool (read-only)
  Raw: True when window is open.

- `width` → int (read-only)
  Raw: X position of left edge of the windows contents. Ignores borders if they are present. Expressed in points or pixels, depending on the 'DPI Scaling' parameter setting.

- `contentY` → int (read-only)
  Raw: Y position of bottom edge of the windows contents. Ignores borders if they are present. Expressed in points or pixels, depending on the 'DPI Scaling' parameter setting.

- `contentWidth` → int (read-only)
  Raw: Width of windows contents. Ignores borders if they are present. Expressed in points or pixels, depending on the 'DPI Scaling' parameter setting.

- `contentHeight` → int (read-only)
  Raw: Height of windows contents. Ignores borders if they are present. Expressed in points or pixels, depending on the 'DPI Scaling' parameter setting.

### Methods
- `setForeground()` → bool
  Raw: Activates the window, sets it to the foregound and other visual cues. Sets focus and increases process priority.

## ZedTOP_Class
Operator: ZedTOP (TOP)

### Members
- `calibrationParameters` → dict (read-only)
  Raw: Dictionary containing the intrinsics and extrinsic stereo parameters for unrectified/distorted images. This includes focal point, optical center, distortion, field of view, and stereo transformation m

- `calibrationParametersRaw` → dict (read-only)
  Raw: Dictionary containing the intrinsics and extrinsic stereo parameters for unrectified/distorted images. This includes focal point, optical center, distortion, field of view, and stereo transformation m

