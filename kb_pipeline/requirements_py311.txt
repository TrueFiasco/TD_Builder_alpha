# BUG-018: Python 3.14+ is NOT supported (ChromaDB uses Pydantic V1)
# Requires: Python >=3.10,<3.14

chromadb==0.5.23
sentence-transformers==5.2.0
numpy==2.3.5
scikit-learn==1.8.0
scipy==1.16.3
mcp

# Notes:
# - This project also installs transitive deps (torch/transformers/onnxruntime, etc.).
# - If Chroma telemetry causes noise, keep `posthog<4` pinned in the venv.
# - BUG-018: ChromaDB's Pydantic V1 dependency is incompatible with Python 3.14+

