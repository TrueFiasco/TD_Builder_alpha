# TD-MCP Alpha Release Requirements
#
# DEPRECATED for normal installs — the root `pyproject.toml` is now the single
# source of truth. From the repo root, run:
#     pip install -e ".[api,dev]"
# This file is kept only for reference / direct subsystem use.

# MCP Server Framework
mcp>=1.0.0

# Anthropic SDK (for agent features)
anthropic>=0.18.0

# Vector Search
sentence-transformers>=2.2.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Knowledge Graph
networkx>=3.0

# Validation
jsonschema>=4.17.0

# Config
pyyaml>=6.0

# Note: SentenceTransformer model (~1.5GB) downloaded on first run
