"""TD Builder CLI entry points (alpha).

Thin launchers that bootstrap the engine import roots and delegate to the
unified_system CLI implementations. Exposed as console scripts via
pyproject.toml: td-validate / td-convert / td-build.
"""
